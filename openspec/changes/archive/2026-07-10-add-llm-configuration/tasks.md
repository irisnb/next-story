## 1. Rust LLM 配置核心

- [x] 1.1 定义 Rust LLM 配置数据结构，包含 API 地址、API Key、模型名和公开加载结果。
- [x] 1.2 添加应用本地配置路径解析，用于保存应用级 LLM 配置 JSON 文件，并确保它位于用户笔记本之外。
- [x] 1.3 实现配置校验：缺少 API 地址、缺少 API Key、缺少模型名、API 地址格式无效。
- [x] 1.4 实现本地 LLM 配置文件的原子保存和加载。
- [x] 1.5 实现 OpenAI-compatible 测试连接逻辑，使用配置的 API 地址、API Key 和模型名。
- [x] 1.6 将保存、加载、测试失败映射为简短可读的用户错误信息。

## 2. Tauri 命令边界

- [x] 2.1 在 `src-tauri/src/lib.rs` 暴露加载、保存、测试 LLM 配置的 Tauri commands。
- [x] 2.2 将新 commands 注册到 Tauri invoke handler。
- [x] 2.3 确保 commands 委托给 Rust LLM 配置模块，不在命令函数里直接写文件或写 HTTP 逻辑。

## 3. 前端 API 和类型

- [x] 3.1 添加 TypeScript 类型，覆盖可编辑 LLM 配置、已加载配置状态和测试连接状态。
- [x] 3.2 添加加载、保存、测试 LLM 配置 commands 的前端薄封装。
- [x] 3.3 确保前端 API 封装不包含文件系统、provider 分支或笔记本写入逻辑。

## 4. LLM 配置界面

- [x] 4.1 添加一个可从现有应用壳进入的最小 LLM 配置视图。
- [x] 4.2 添加 API 地址、API Key、模型名输入项，其中 API Key 使用 password 类型输入框。
- [x] 4.3 添加明确提示：开发阶段 API Key 会以本地明文方式保存。
- [x] 4.4 有已保存配置时加载到界面；没有配置时显示空状态。
- [x] 4.5 实现保存配置动作，包含本地校验、等待期间禁用按钮、保存成功/失败状态。
- [x] 4.6 实现测试连接动作，包含等待期间禁用按钮和可读成功/失败状态。
- [x] 4.7 确保界面不添加 AI 面板、选区召唤、插入、替换、改写、多模型或多 provider 控件。

## 5. 自动化验证

- [x] 5.1 添加 Rust 测试，验证保存/加载 LLM 配置不会触碰草稿本或正文本文件。
- [x] 5.2 添加 Rust 测试，覆盖配置缺失或配置格式无效时的校验失败。
- [x] 5.3 添加 Rust 测试，使用受控本地/mock HTTP endpoint 或等价的确定性测试 seam 覆盖测试连接成功和失败。
- [x] 5.4 通过完整 TypeScript 类型检查覆盖新增 commands 和 UI 状态。

## 6. 手动验证

- [x] 6.1 成功运行项目完整检查命令。
- [x] 6.2 启动桌面应用，验证 LLM 配置视图可以保存并重新加载配置。
- [x] 6.3 使用用户提供的真实 OpenAI-compatible 配置运行测试连接，并验证成功状态。
- [x] 6.4 验证连接失败场景显示可读错误，并且不会修改草稿本或正文本。

## 7. 归档前审查修复

- [x] 7.1 添加 URL 安全回归测试：缺主机、远程 HTTP、用户信息、query、fragment、完整 chat-completions 路径。
- [x] 7.2 添加响应结构回归测试：合法非空 choices 成功；HTML、空正文、空 choices、错误对象和 204 失败。
- [x] 7.3 将连接测试改为异步 reqwest client，禁用自动重定向，并稳定隔离本机 mock 测试。
- [x] 7.4 实现结构化 URL 校验：远程 HTTPS、本机回环 HTTP、基础地址规范化。
- [x] 7.5 添加设置来源和未保存文本保护测试，并实现返回原页面行为。
- [x] 7.6 添加表单 busy/valid 与过期 refresh 状态测试，并实现并发保护。
- [x] 7.7 更新 LLM 配置界面说明，明确基础地址、HTTPS 和本机 HTTP 规则。
- [x] 7.8 更新 README 当前能力、源码结构、命令数量和统一“LLM 配置”命名。
- [x] 7.9 重跑完整检查、OpenSpec 严格校验、重复 Rust 网络测试并重建 release exe。
- [x] 7.10 完成归档前最终复查并取得明确归档建议。
