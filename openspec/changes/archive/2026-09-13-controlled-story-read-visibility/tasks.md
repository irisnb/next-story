## 1. 数据模型与迁移

- [x] 1.1 为文档节点增加默认允许的二元 AI 可见性字段，保持文件夹没有可见性、继承、覆盖或优先级字段。
- [x] 1.2 提升作品结构版本并实现旧作品迁移；验证缺少新字段的现有文档按允许查看处理，迁移失败不产生半写文件或正文变化。
- [x] 1.3 增加 Rust 序列化、往返和迁移测试，覆盖新建文档默认允许、开关持久化、移动/删除/恢复后状态稳定及旧参考优先级概念不回归。

## 2. 统一受控只读作品服务

- [x] 2.1 将阶段 1 的只读材料校验收敛为生产作品服务，统一校验作品、文档、回收站、AI 可见性、版本、范围和未保存快照身份。
- [x] 2.2 实现提供给 AI 路径的目录投影：只返回允许节点及必要文件夹路径，并返回匿名隐藏文件数量，不泄露隐藏文档名称、ID、路径或所在文件夹。
- [x] 2.3 在作品锁保护下提供前端可调用的受控 Tauri 查询/更新命令，并保证错误失败关闭、不会返回被拒材料的敏感细节。
- [x] 2.4 补充 Rust 负向测试，覆盖跨作品、回收站、隐藏文档、游离节点、错误版本、非法范围、伪造快照和正文零写回。

## 3. 文件管理可见性界面

- [x] 3.1 在文件管理区域为文档节点增加符合现有设计的 AI 可见性开关和说明；文件夹不显示该开关。
- [x] 3.2 将开关连接到受控 Tauri 命令，处理保存中、成功和失败状态；失败时恢复原状态并显示中文可读提示。
- [x] 3.3 增加前端测试，覆盖默认允许、单篇切换、切换失败、文档移动/重命名后状态保持，以及开关不修改正文。

## 4. 未保存快照与选区授权

- [x] 4.1 统一前后端材料身份契约，使未保存快照携带作品 ID、文档 ID、版本和合法 Tiptap JSON，并由 Rust 受控入口校验。
- [x] 4.2 扩展选区快照与 AI 首轮请求，使选区携带来源作品、文档和版本身份；在预检及实际发送前经过同一可见性授权。
- [x] 4.3 隐藏文档、跨作品或版本失效的选区不得发送给 AI，并显示不泄露隐藏身份的中文提示。
- [x] 4.4 增加前后端测试，覆盖允许选区、隐藏选区、预检期间关闭权限、切换作品和伪造身份，证明裸选区文本不能绕过授权。

## 5. 讨论材料出处与权限变化隔离

- [x] 5.1 演进讨论档案版本，保存文档身份、材料类型、版本、所属轮次和是否进入模型上下文等最小出处元数据，不复制保存完整正文。
- [x] 5.2 在关闭文档可见性时识别受影响讨论并永久标记为材料权限已变化；旧讨论内容继续可查看但不得继续追问或自动恢复，提示中只说明有一份隐藏文件而不暴露其身份。
- [x] 5.3 阻止受限讨论沿原 DSH 会话继续、崩溃恢复或重开时重放历史；为用户提供新建干净讨论的继续路径。
- [x] 5.4 重新允许文档查看时不解除已永久受限的旧讨论（仍只能查看历史，不能继续追问或自动恢复）；缺少材料出处的旧档案按可查看但不可自动重放的保守策略处理。
- [x] 5.5 增加讨论保存、重开、驱动崩溃恢复和权限反复切换测试，验证显示历史保留但隐藏材料不会再次发送给 DSH。

## 6. 调度、边界守卫与产品提示

- [x] 6.1 让排队请求在实际派发前重新校验材料权限；已发送请求不声称可撤回，权限变化后的新增读取必须重新授权。
- [x] 6.2 完成文件隐藏、选区拒绝、受限讨论查看与新建讨论等中文提示，并核对提示不泄露隐藏文件名称或路径。
- [x] 6.3 扩展能力网关和生产配置守卫测试，证明阶段 4 没有注册 DSH 读取工具、通用文件工具或任何作品写入能力。
- [x] 6.4 验证阶段 4 没有改变普通提问的自动材料组装，没有实现跨文档搜索、Agent 补读、摘要、检索或记忆。

## 7. 联合验证与文档同步

- [x] 7.1 运行前端单元测试、TypeScript 检查、Rust 测试和格式检查，修复本 change 引入的失败。
- [x] 7.2 用临时作品执行真实链路走查：默认允许、文件管理关闭、AI 目录匿名计数、隐藏选区拒绝、旧讨论受限、新讨论可开始及正文哈希不变。
- [x] 7.3 以最小自动化断言验证不泄露（可判定）：讨论档案 provenance 字段只含 provenance 元数据且不含 snapshot/bodySnapshot/content/documents 正文副本字段；受控可见性/目录命令错误不泄露作品路径或文档身份；保留已有审计修复的 stderr 与配置路径测试。
- [x] 7.4 更新 README 与方向状态文档，诚实标注阶段 4 已完成的只读基础，以及阶段 5 自动现场材料、阶段 6 Agent 按需补读仍未实现。
