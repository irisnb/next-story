# 暂停说明

- 状态：已撤回并暂停，未实施，未归档。
- 暂停原因：项目优先确认并同步使命与能力边界；本 change 的第一阶段方法库、方法卡内容和提问策略仍需继续讨论。
- 当前事实：这些 proposal、design、delta spec 和 tasks 只是讨论成果，不属于 `openspec/specs/` 已实现真相源。
- 恢复条件：当前活动 change 完成并归档后，用户明确决定继续本方向；恢复前必须依据最新方向文档重新审阅范围，不得默认原方案继续有效。
- 恢复方式：将整个 `refine-selection-ai-questions` 目录移回 `openspec/changes/`，确认没有其他活动 change，再运行 OpenSpec 校验。
