## 1. 验证边界与固定材料

- [x] 1.1 建立固定作品 fixture，覆盖允许文档、隐藏文档、回收站文档、另一部作品和带未保存修改的文档
- [x] 1.2 定义验证运行的作品、讨论、会话、轮次、消息和请求身份，并让证据记录使用同一身份模型
- [x] 1.3 固化阶段 1 的 DSH 配置 allowlist，确认通用文件、shell、网络、子代理、无限循环和作品写入能力保持禁用

## 2. DSH Agent Loop 与只读桥接

- [x] 2.1 用固定模型或可控替身启动最小 DSH Agent Loop，并验证直接回应的文本流和唯一终态（确定性替身 sidecar/driver/adapter.mjs，非实机 DSH）
- [x] 2.2 实现验证用受控只读请求适配层（sidecar/validation/bridge.mjs 对固定 fixture 离线校验后返回结构化材料；Rust 侧 story_material.rs 为纯函数只读边界、capability_gateway.rs 为工具授权分类，二者均为离线可审计的自由函数/单元测试，非注册 Tauri 命令、未接入生产 driver 工具协议）
- [x] 2.3 为允许文档、另一部作品、隐藏文档、回收站文档、旧版本和未知工具补充失败关闭测试
- [x] 2.4 验证未保存快照的作品身份、文档身份、版本和可见性校验，并确认返回最新快照而非旧磁盘稿

## 3. 工具事件与用户确认

- [x] 3.1 记录工具开始、成功、失败、取消、超时、用户确认等待、确认和拒绝事件
- [x] 3.2 验证用户拒绝扩大读取范围后返回结构化拒绝，Agent 不把被拒材料当作已读内容（确定性替身）
- [x] 3.3 验证授权读取完成后材料回到同一轮 Agent Loop，并能继续生成回应（确定性替身）
- [x] 3.4 验证每个工具调用只有一个终态，取消或超时后的迟到结果不会重新打开调用

## 4. 多会话隔离、取消与恢复

- [x] 4.1 让两个会话并行运行交错事件，并验证各自只收到自己的文本和工具结果
- [x] 4.2 取消一个请求时验证另一个请求继续运行，且取消、失败和超时状态不互相传播
- [x] 4.3 注入迟到文本和工具事件，验证已结束请求、其他作品和新请求均不被污染
- [x] 4.4 模拟驱动重启或连接中断，记录工具材料和事件当前可恢复字段，并验证明确降级路径（文本轮次可恢复、工具材料明确不可恢复，见 adapter.exportRecovery）

## 5. 等待基线与证据

- [x] 5.1 记录直接回应、一次只读补读、用户确认后恢复和并行请求的提交、首字、排队、工具等待及完成时间
- [x] 5.2 区分进度提示与实际模型文本，标记失败或计时不可用的运行，不生成虚假的延迟结论
- [x] 5.3 汇总验证结果、失败样本、可复用 DSH 能力和必须新增的桥接，形成阶段 2 之前的接入结论（确定性汇总 sidecar/validation/summary.mjs；真实模型延迟/失败样本不可用，明确标记为未验证）

## 6. 安全回归与交付门槛

- [x] 6.1 回归现有临时会话的启动、追问、流式回应、取消、崩溃重启和迟到结果隔离行为（确定性替身协议面回归，非实机 driver）
- [x] 6.2 验证所有验证场景不会改变作品正文、内容树或保存接口数据
- [x] 6.3 检查验证配置和测试 fixture 不会被生产路径默认启用，并确认回滚只需移除验证配置（静态守卫 sidecar/validation/tests/config-guard.test.mjs：生产 cordis.driver.yaml 危险工具 disabled:true、driver.mjs 不引用验证模块/不注册工具）
- [x] 6.4 运行项目规定的测试与格式检查并整理可审计证据：67 项验证测试通过；npm run check 通过；npm run test:rust 通过（116 passed，1 ignored 为实机 DSH 探针、未实机运行）；git diff --check 通过。证据均为离线确定性验证，未接入生产 Rust/Tauri 读稿路径
