## 1. DOM 契约与结构调整

- [x] 1.1 调整 `index.html` AI 面板骨架：合并 `#ai-direct-question` 与 `#ai-conversation` 为单一对话容器，删除首轮流式回答独立区块（`#ai-direct-question-response`、`#ai-direct-question-loading`），新增空状态欢迎语节点
- [x] 1.2 将输入区（直接提问输入 + 可选重点提示框 + 追问输入）移出消息流，改为面板底部固定区域；直接提问与追问共用同一底部位置
- [x] 1.3 同步更新 `src/dom.ts` 的 `AiPanelDom` 契约：移除已删节点引用，新增欢迎语节点引用，保持每步可编译

## 2. 视图模型统一

- [x] 2.1 调整 `src/ai-panel-view-model.ts`：`directQuestion` 存在时也产出统一对话视图（冻结材料映射为用户轮次 + 生成中状态），删除 `directQuestion` / `conversation` 互斥分支
- [x] 2.2 核对首轮失败、重试、缺配置、请求未接受等状态下统一视图的产出正确（对照 `ai-thinking-panel` spec 既有 scenario），必要时最小限度调整 `src/ai-panel-reducer.ts` 输出结构但不改迁移语义

## 3. 渲染层与样式

- [x] 3.1 调整 `src/ai-panel.ts` 渲染逻辑：首轮与追问统一渲染进对话流（用户消息 → 生成中占位 → 流式回复原地展开），删除旧双路径渲染代码
- [x] 3.2 调整 `src/styles.css`：面板改为纵向 flex（消息区 `flex: 1` 单滚动容器 + 输入区 `flex: none` 固定底部），删除 `max-height: 32vh` 内层滚动
- [x] 3.3 更新消息样式：用户消息浅色底色块通栏（移除左侧强调竖线），AI 回复无边框无底色裸排；轮次间距大于轮内间距；新增欢迎语样式
- [x] 3.4 确认窄窗口（<=760px）抽屉形态在新 flex 结构下正常显示

## 4. 吸底滚动

- [x] 4.1 在消息区滚动容器实现贴底检测（滚动事件维护贴底标记，阈值约 40px）
- [x] 4.2 增量渲染（`append_stream_text` 路径）后按贴底标记自动滚动到底部；用户上滚暂停跟随、滚回底部恢复；面板收起期间不滚动，重新展开时置于底部

## 5. 验证

- [x] 5.1 逐条验证 specs 增量中的全部 scenario（首轮发送即入对话流、完成无跳变、输入框位置恒定、消息样式、窄窗口抽屉、吸底跟随/暂停/恢复、收起期间、欢迎语出现与消失、首轮/追问的开始与失败状态位置）
- [x] 5.2 回归验证既有语义未破坏：重试使用原冻结快照、缺配置引导、迟到结果隔离、取消生成、崩溃恢复重放显示、纯文本渲染（Markdown/HTML 按普通文字显示）
- [x] 5.3 确认欢迎语文案不承诺能力或结果，交用户过目确认
