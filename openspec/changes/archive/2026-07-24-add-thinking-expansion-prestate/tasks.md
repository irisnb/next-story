## 1. 现状确认

- [x] 1.1 核对选区浮动入口、AI 面板状态、请求协调和相关测试文件位置，确认本 change 需要修改的最小文件集合
- [x] 1.2 核对现有`及时召唤`行为，确认改名和新增入口不会改变它的立即请求、冻结选区和无初始输入边界

## 2. 选区浮动入口

- [x] 2.1 将有效选区附近的浮动入口从单一`召唤 AI`调整为跟随选区末端的小圆点触发器
- [x] 2.2 点击小圆点后，在触发器右侧展开仅包含`及时召唤`和`思维扩展`的小菜单，并让小圆点颜色加深
- [x] 2.3 保持无效选区、空白选区、选区取消、切换本子和锚点滚出视口时隐藏入口的既有行为
- [x] 2.4 点击任一开启动作后隐藏本次活选区入口和小菜单，并确保面板展开或收起不会让旧入口自动重新出现

## 3. 冻结选区与面板预备态

- [x] 3.1 为`思维扩展`点击动作冻结本子类型、选区原文和起止位置，并确保后续编辑、改选区或切换本子不改变该锚点
- [x] 3.2 在右侧 AI 面板新增`思维扩展`预备态，显示`思维扩展`、`已选中 N 字`、方向输入框和`开始`按钮
- [x] 3.3 将方向输入框 placeholder 设置为`写下你想追的方向`，进入预备态后自动聚焦输入框
- [x] 3.4 确保预备态不显示示例问题、能力说明、教学卡片或“AI 可以帮你”类功能解释

## 4. 思维扩展请求与临时对话

- [x] 4.1 点击`开始`时允许方向为空，并只使用冻结选区原文发起首次请求
- [x] 4.2 点击`开始`时允许带方向，并把方向作为用户希望探索的角度传入首次请求
- [x] 4.3 确保`思维扩展`请求不附加选区前后文、当前本子全文、摘要、作品元数据、AI 内容库、历史会话或用户确认的作品信息
- [x] 4.4 请求被接受后进入当前单条线性临时对话，复用成功回复、追问开放、收起保留、切换作品清空、重启不恢复和迟到结果隔离规则
- [x] 4.5 确保`思维扩展`输出只显示在 AI 面板中，不提供插入、替换、应用到正文或等价写回入口

## 5. 测试与验证

- [x] 5.1 增加或更新入口测试，覆盖小圆点入口、右侧小菜单、两个开启动作、及时召唤立即请求、思维扩展进入预备态和点击后隐藏旧入口
- [x] 5.2 增加或更新预备态测试，覆盖文案、字数、自动聚焦、空方向开始、带方向开始和预备态期间编辑器变化不改锚点
- [x] 5.3 增加或更新请求组装测试，确认思维扩展只包含冻结选区和可选方向，不包含未授权上下文来源
- [x] 5.4 运行相关前端测试、类型检查、构建和浏览器视觉验证，确认本 change 没有引入回归
