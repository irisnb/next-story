## 1. 协议真相源与命名统一（P2-5 / P2-6 / P2-9）

- [ ] 1.1 新增 `sidecar/driver/protocol.json`：机器可读协议定义（事件名、方向、字段），覆盖既有 v1 全部出站/入站事件（含 `message_sent`）与新增 `tool_call` / `tool_result`
- [ ] 1.2 `driver.mjs` 与 `adapter.mjs` 改为从 `protocol.json` 读取事件定义；替身旧名 `story.list` / `story.read_document` / `story.read_snapshot` 统一为 `story-list` / `story-read` / `story-search` / `story-request-reading`
- [ ] 1.3 Rust 契约测试：解析 `protocol.json`，断言与 `dsh_driver.rs` 事件常量一致
- [ ] 1.4 禁用能力清单单一真相源：一份清单文件生成 `gen-config.mjs` 的 DENY_IDS、`capability_gateway.rs` 禁用清单与两端测试
- [ ] 1.5 离线驱动回归更新：替身断言覆盖生产事件集（含 `message_sent`）

## 2. 网关真实授权与宿主执行器（P2-10 / P2-7）

- [ ] 2.1 `capability_gateway.rs`：`CoreCapability::ToolCall` 真实授权——仅放行 `story-list` / `story-read` / `story-search` / `story-request-reading`，其余一律拒绝
- [ ] 2.2 新增宿主工具执行器：三个读取工具只注入窄化 `StrictStoryReader` 接口，类型层面不暴露保存/恢复/迁移/删除函数；node 驱动进程不得自行读取作品文件
- [ ] 2.3 `story-search` 工具：复用 `story_search.rs` 字面检索内核（NFKC 等），Agent 提供检索词，每次调用输出硬上限初值沿用 A 部分常数（5 文档 / 10 片段 / 前后各 120 字符）
- [ ] 2.4 `story-request-reading` 控制工具：宿主拦截转为面向用户的授权请求，不携带作品数据
- [ ] 2.5 失败关闭测试：隐藏 / 回收站 / 越权 / 待恢复事务 / 未授权状态下四个工具全部拒绝、零副作用、作品逐字节不变

## 3. 授权状态与档案持久化

- [ ] 3.1 `conversation_store.rs`：讨论档案新增按需补读授权状态（未授权 / 已授权及时间）与补读读取出处（文档身份、版本、阅读程度、所属轮次、是否进入模型上下文），沿用原子写入与串行保存
- [ ] 3.2 旧档案兼容：缺少新字段时视为未授权并正常打开，不自动重放给模型
- [ ] 3.3 宿主逐次校验接线：每次 `tool_call` 重新校验讨论授权状态 + 作品身份 + 回收站 + AI 可见性 + 版本 + 待恢复事务
- [ ] 3.4 Rust 测试：授权状态跨重启恢复、读取出处最小元数据、旧档案兼容

## 4. 驱动工具通道接入

- [ ] 4.1 `driver.mjs` 注册 Agent 工具面（四个工具），实现仅为协议桥接转发；工具结果按 DSH 会话事件回填
- [ ] 4.2 `tool_call` / `tool_result` 事件接入：授权等待期间轮次挂起且不产生模型请求；用户决定后以工具结果回填继续原问题
- [ ] 4.3 停止与取消：等待授权或等待工具结果的轮次可被停止生成取消；会话/讨论身份失效时迟到工具结果丢弃
- [ ] 4.4 提示词更新：材料不足先请求授权（说明原因）；及时召唤首轮只用选区
- [ ] 4.5 宿主硬门禁：及时召唤首轮与未授权讨论的补读 `tool_call` 一律结构化拒绝
- [ ] 4.6 重生成 `cordis.driver.yaml` 并通过既有驱动队列回归

## 5. 版本固定、阅读程度与熔断

- [ ] 5.1 轮内版本固定表（讨论, 轮, 文档 → 版本）：`story-list` 返回版本，`story-read` 携带版本，不匹配返回结构化 `story_version_changed`，本轮停读该文档、下一轮读新版
- [ ] 5.2 阅读程度判定：按轮累计读取范围对照已保存正文长度，输出搜索片段 / 局部阅读 / 完整阅读
- [ ] 5.3 同轮去重：宿主按轮记录已提供（文档、版本、范围），重复请求返回已读提示（附出处）；跨轮不屏蔽
- [ ] 5.4 宿主侧熔断：按轮工具调用计数 + 累计时长阈值，超限返回结构化「补读已停止」，模型基于已有材料收束；阈值配置化
- [ ] 5.5 Rust 测试：版本固定与混读拒绝、阅读程度判定、同轮去重、熔断触发与正常路径不受扰

## 6. 前端：授权交互与过程呈现

- [ ] 6.1 授权请求卡片：显示请求原因与权限边界（仅本讨论、只读、不再重复询问、可随时关闭），提供允许 / 拒绝；允许后自动继续、拒绝后有限回答；措辞不表述为「现在才允许 AI 查看作品」
- [ ] 6.2 讨论内授权开关：随时开 / 关；关闭立即阻止后续读取并明示「不清除已读内容」；停止生成不改变授权状态
- [ ] 6.3 补读过程轻量状态 + 可展开详情（正在搜索 / 已读文档列表；不展示模型内部推理）
- [ ] 6.4 「本次参考了什么」扩展：按需补读文档与阅读程度展示
- [ ] 6.5 关闭文档 AI 可见性前的影响提示：受影响讨论说明（永久只读、旧出处脱敏），确认后生效
- [ ] 6.6 等待中退出按既有中断轮处理：验证重开后该轮显示中断、经授权开关开启后可重试或重发（复用既有机制，不新增恢复流）
- [ ] 6.7 前端测试：授权流程、开关语义、过程显示、参考记录、可见性提示、中断轮重试路径

## 7. max_tokens 配置链（P2-8）

- [ ] 7.1 LLM 配置新增可选 `max_tokens` 字段，经 `dsh_driver.rs` 透传 `driver.mjs --max-tokens`；缺省维持 131072；配置界面与校验同步

## 8. 端到端验证与回归

- [ ] 8.1 离线验证套件扩展：工具循环、授权请求 / 允许 / 拒绝 / 关闭、协议契约测试全过
- [ ] 8.2 真实链路驱动级回归（真实端点）：工具循环 + 授权流程 + 熔断 + `message_sent` 回执断言
- [ ] 8.3 负向能力验证更新：常驻进程内写文件 / 执行命令 / 自行读取作品 / 未知工具全部失败且零磁盘副作用
- [ ] 8.4 全量回归：前端测试、Rust 测试、typecheck、ESLint、生产构建、OpenSpec 全量严格校验
- [ ] 8.5 熔断阈值与 `story-search` 每调用上限初值校准：依据验证结果写入配置默认值，并在设计文档记录依据（不写成产品效果承诺）
