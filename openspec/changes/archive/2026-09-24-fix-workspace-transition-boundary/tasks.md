> 核收状态（2026-09-24）：实施和自动测试证据见 `verification.md`。本轮仅登记证据明确的核收项，其余实施项仍需逐条完成证据映射，不能把未勾选直接解释为未实现，也不能批量当作已验收。5.3/5.4 人工验收和第 6 节仍未完成；总检查失败原因单独保留。

## 1. 审核与实施前置门禁

- [x] 1.1 主控审阅 proposal/design/spec deltas，核对只覆盖报告第 10 节第一项 F01＋F02＋F08；用户最终确认忙时不受理/不排队、现有取消边界、暂停时机和提示方式后，才允许进入 apply。后续用户已确认方案并批准实施，已进入核收阶段；不补写未记录的批准日期或原话。
- [ ] 1.2 实现代理开工前重新核对唯一活动 change、工作区现状及生产接口，明确准备/已提交/失败/取消/失效/忙碌结果和操作所有权；不把本文的待审设计当作已实现，不新增第二个 change。

## 2. F01：正文切换保护与读取提交

- [ ] 2.1 将只读审计脚本 `tmp/project-audit/editor-race.test.ts` 的实际 `setupEditor`＋可控延迟场景转为 `tests/editor.test.ts` 正式安全回归，先覆盖保存等待、读取等待、原实例/输入/历史保持；断言安全行为，不能以“缺陷被复现”的通过结果证明修复。
- [ ] 2.2 在现有编辑器适配器/内核补足组合输入自然完成与正文修改许可的窄接口；覆盖最终正文更新后再暂停，禁止强制销毁组合态或保存未确认拼音；不升级编辑器依赖。
- [ ] 2.3 将暂停接到全部正文修改入口：输入、删除、格式/链接、撤销重做、查找替换、粘贴剪切、拖放与快捷键；异步剪贴板按实例及许可代次复核，失败恢复同一实例后也不补执行旧命令。补对应内核/入口测试，不能只断言 inert/disabled。
- [ ] 2.4 在切换保存前取得操作所有权并保持保护到最终提交/失败；普通手动保存仍可继续编辑。若有在途手动保存，等待后复核 dirty 并保存剩余修改；提交前发现新修改则安全中止。用 `editor-save-state.test.ts`、`editor-persistence.test.ts` 锁定快照、错误和真实保存状态。
- [ ] 2.5 拆开 `editor-document-session.ts` 的候选读取/校验/构建与成功安装；失败、失效不得伪装成功，准备期不改身份、记忆或交互模块。用 `editor-document-session.test.ts` 验证读取/解析/构建失败保留旧实例，合法空树与读取失败区分，旧 finally 不解新保护。
- [ ] 2.6 将文档列表接入一次只受理一个切换的规则，忙碌判断先于当前文档早退，显示目标和暂停原因；补快 A→B→C、A→B→A、不排队、空闲点击当前文档无副作用回归。
- [ ] 2.7 将树回落的正文读取接入同一保护与准备/提交边界；保留删除确认取消时不接受树、读取失败保留原树/输入、合法空态的行为；验证切文档及树回落不重置讨论、不关闭窗口、不改讨论绑定。
- [ ] 2.8 实现代理运行 F01 定向回归（`editor.test.ts`、`editor-document-session.test.ts`、`editor-save-state.test.ts`、`editor-persistence.test.ts` 及本节必要的内核/入口用例），记录命令、结果和未覆盖项供主控复核。

## 3. F02：文件管理原身份与刷新顺序

- [ ] 3.1 将只读 `tmp/project-audit/file-race.mjs` 的 A 树迟到场景转入 `tests/file-management.test.ts`，断言迟到树不渲染、不通知当前编辑器、不触发当前作品权限重算；不运行原缺陷断言作为修复证据。
- [ ] 3.2 为文件管理装载/重开/卸载引入装载代次，为刷新引入请求序号，为状态与收尾保留操作所有权；同路径重开仍使旧请求失效。最新刷新失败保留原树，旧成功不能兜底覆盖。
- [ ] 3.3 审核新建文档/文件夹、重命名、移动行内确认、删除、恢复的捕获路径与节点身份；每个 await 后复核，旧回调不读当前可变路径、不清新状态。已发后端操作只作用原作品，不伪称磁盘回滚。
- [ ] 3.4 修复 AI 可见性影响查询→确认→写入→刷新链的原身份保护，在查询后和确认续接处检查；保持权限算法与关闭影响文案原义，不纳入 F03/F05/F06。
- [ ] 3.5 给宿主树通知带上可核对身份，阻止切换保护期内的树/文档提交；仅暂存最新原装载候选，取消/失败后重新核验再接受，成功离开丢弃。同作品重开等待已发结构写入并重取树，不新增对外队列。
- [ ] 3.6 实现代理运行 `file-management.test.ts` 及树同步定向用例：A→B、A→欢迎页、B→A 迟到、A→B→A、同装载反序刷新、最新失败旧成功、失败归属、旧 finally、可见性查询期间切作品与行内旧确认。记录服务调用的实际路径和宿主通知次数。

## 4. F08：真实应用装配、取消与成功副作用

- [ ] 4.1 将 `main.ts` 的作品接入改为可等待真实完成的链，打通 `new-project-form.ts`、`project-leave-flow.ts` 与编辑器候选准备/提交；不允许 void 回调、内部吞错或失效正常返回触发成功后续。
- [ ] 4.2 在应用装配入口统一打开/最近作品/新建提交/返回欢迎页与切文档的操作所有权，跨 await 保持保护；禁用和处理器检查并用，额外导航不排队，模块导航不被迟到打开强行拉回。
- [ ] 4.3 实现无异步间隙的共同提交：候选准备成功后才安装两侧身份与树、发布视图、更新文档记忆、切写作模块；仅成功分支恰一次 AI 作品初始化和最近记录调用。最近记录失败不回滚或重复初始化。
- [ ] 4.4 同作品重开在授权保存完成后读取目标正文及必要的新树，禁止预读旧正文覆盖新保存；加入未保存格式、在途普通保存、已发结构操作和同路径新装载回归。
- [ ] 4.5 保留目录选择取消、尚未提交的新建表单取消和离开确认取消；新建提交期间禁用表单取消。创建成功但装载失败保留表单与文件夹，明确引导打开重试，不重复创建、不记打开成功。
- [ ] 4.6 返回欢迎页走组合结束→暂停→离开确认/保存→共同卸载与失效；忙时返回不受理、不排队。取消、保存失败、不保存后读取失败、保存后读取失败均保留应保留的旧状态；卸载/销毁后迟到结果不复活页面，不扩修关闭协调器。
- [ ] 4.7 在 `project-flow-race.test.ts` 或由它调用的正式装配夹具中经过生产入口和真实应用协调回调、实际 `setupEditor`/文件管理；只在服务边界注入延迟，不用同步 onProjectReady mock 替代接线。断言正文悬而未决时上层仍忙、失败确实到达上层、无半套作品、打开/新建/最近入口成功真正完成、成功恰一次 AI 初始化。
- [ ] 4.8 更新 `project-leave-continuation.test.ts`、`leave-safety.test.ts`、`recent-works.test.ts`，使延迟期间编辑/冻结证据落在实际被守卫的实例，而非无关 SaveState；补取消、返回欢迎页、同作品重开、成功与失败的记忆和最近记录归属。
- [ ] 4.9 实现代理运行本组装配及离开相关定向回归，提交结果给主控复核。先前测试的“新建覆盖旧打开”预期按经用户批准的忙时不受理规则调整，同时保留底层失效防护测试。

## 5. 总验收与真机证据

- [x] 5.1 主控执行仓库既有总检查并复核全部定向结果，明确总检查实际覆盖与未运行项；不因当前脚本漏科目就宣称全覆盖，不顺手纳入 F10 工程卫生修改。总检查失败后由验证代理独立补跑后续科目，主控核收；执行完成不等于 `check` 通过，见 `verification.md`。
- [x] 5.2 主控独立核验真实装配链 Promise 与失败传播、身份一致、成功副作用次数，至少复查打开/新建正文延迟和失败、同作品重开、旧树/旧 finally 迟到；不能仅接受表单单元 mock 的通过记录。主控独立复跑装配与记忆存储 36 条通过，并结合源码差异及 Oracle 复核；adapter 与真机覆盖限制见 `verification.md`。
- [ ] 5.3 具备环境的验收者在 Windows Tauri/WebView2＋微软拼音使用合成作品执行 design 的组合输入人工矩阵；记录系统/WebView2/输入法版本、操作步骤、预期/实测和录屏或截图位置，主控核收。未执行则保持本项未完成并明确“待验”，模拟 composition 事件不能代签。
- [ ] 5.4 实现代理与主控核对暂停提示、目标名与当前名、禁用原因、失败恢复焦点/选区/历史；確認保护不锁死离开确认三选项、不抢 AI 输入焦点、不重做 UI。
- [ ] 5.5 主控运行 `openspec validate fix-workspace-transition-boundary --strict` 并检查最终 status、delta 与实现一致；向用户列出验证通过、失败和待验项，未完成人工验收不冒充全部验收通过。

## 6. 分笔提交、文档入库与归档（届时另取授权）

- [ ] 6.1 后续 apply 按报告第 10 节按发现分笔组织 F01、F02、F08，每笔包含相应正式回归并注明共享接口依赖，保持可追溯回退；每次提交/其他 git 写操作前另获显式授权并加载 git-master，检查状态、精确暂存、使用项目风格中文说明。此任务不授权现在提交或推送。
- [ ] 6.2 后续文档笔将《方向/开发前全面工程复核-2026-09-23.md》纳入版本库并在 AGENTS.md 文档地图登记，核对不混入无关工作区文件；当前提案阶段不编辑报告或 AGENTS。
- [ ] 6.3 全部必要验收完成并经主控与用户确认后，按 OpenSpec 流程归档本 change；未执行项不能勾为已完成，不能提前归档或开第二个 change。
- [ ] 6.4 归档成功后才更新报告第 10 节第一行状态为完成，附实际归档 change 路径和日期；若需要提交该文档变更，再按显式授权与 git-master 流程执行。
