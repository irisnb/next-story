## 1. 导出模型与依赖

- [x] 1.1 选择并锁定可在 Windows Tauri 构建中使用的 DOCX 生成依赖，补充包配置和许可证记录
- [x] 1.2 定义独立的作品导出序列模型，按内容树顺序表达作品、文件夹、文档和结构化正文
- [x] 1.3 实现已保存作品内容的只读遍历，排除回收站节点并覆盖空作品和嵌套文件夹

## 2. DOCX 生成

- [x] 2.1 实现作品标题、文件夹标题和文档标题的 Word 层级映射
- [x] 2.2 实现段落、编辑器标题、无序列表、有序列表及嵌套结构到可编辑 DOCX 内容的映射
- [x] 2.3 实现可支持的粗体、斜体及其他文字标记映射，并为不可表达属性保留可见文字的降级策略
- [x] 2.4 实现临时文件生成、成功后写入目标路径及失败清理，避免留下不完整导出文件

## 3. Tauri 与前端入口

- [x] 3.1 新增后端只读导出命令，接收作品路径和用户选择的目标路径并返回稳定成功/失败结果
- [x] 3.2 在前端 API 层封装导出命令和中文错误结果
- [x] 3.3 在写作界面增加“导出作品 / Word”入口，使用保存对话框并默认建议作品名称 `.docx`
- [x] 3.4 处理取消、导出中防重复触发、成功提示和未保存版本提示

## 4. 验证

- [x] 4.1 为内容树遍历和导出序列添加单元测试：顺序、嵌套文件夹、空作品和回收站排除
- [x] 4.2 为 DOCX 生成添加测试：文件有效性、标题层级、文字顺序、中文/emoji、段落、列表和文字标记
- [x] 4.3 为失败路径添加测试：取消、读取失败、写入失败、临时文件清理和作品数据不变
- [x] 4.4 运行项目类型检查、测试和生产构建；DOCX 有效性由 OOXML 包结构与关键格式测试覆盖（本环境无法启动 Word 做人工打开验证）
