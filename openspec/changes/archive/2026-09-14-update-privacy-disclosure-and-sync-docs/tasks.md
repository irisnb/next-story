# 任务：诚实告知与文档同步

## 1. 配置页告知文案

- [x] 1.1 重写 `index.html` 设置页三段 `config-warning`：按链路区分（测试连接 / 常规讨论 / 及时召唤）；常规讨论列出自动附带的关注文档现场材料（已保存正文或经校验的未保存快照）、允许目录投影与跨文档字面检索片段；含可见性与回收站生效句、第三方处理句、AI 回复为作品外临时材料的边界句。完整字句先呈用户过目再落盘
- [x] 1.2 在 `tests/llm-config-form.test.ts` 新增告知文案断言，覆盖稳定子串：测试连接只发固定测试语句、常规讨论材料列举（关注文档 / 未保存 / 目录 / 检索）、及时召唤以冻结选区发起、可见性生效、第三方处理；并断言不再出现「只发送问题与选区」类旧表述
- [x] 1.3 运行全量前端测试（`npm run test:frontend`），确认其他读取 `index.html` 的测试（`dom-contract.test.ts`、`ai-panel-dom.test.ts`）未被文案改动破坏

## 2. README 事实对齐

- [x] 2.1 状态段（第 7 行区域）与里程碑表更新：阶段 4 / 5 A 已完成归档、下一步为阶段 6；应用级真实材料 E2E、等待基线、并发上限数值单列为「尚未完成的验证」，不与未实现能力混写
- [x] 2.2 「已实现能力」补文档级 AI 可见性、受控只读、常规讨论自动现场材料与材料出处查看；「当前未实现」移除自动作品上下文，保留 Agent 按需补读
- [x] 2.3 修正隐私相关表述（第 49、69、153 行区域）与第 95 行「AI 参考优先级」措辞错误（内容树是现行基础设施，旧优先级模型已撤销）
- [x] 2.4 「数据保存在哪里」表对齐真实存储：`作品文本/documents/<稳定文档ID>.json`、`next-story-system/content-tree.json`、`next-story-system/project.json`、`next-story-system/conversations/`；旧双本子文件标注为「仅旧作品迁移读取」；「作品与写作」「AI 思考生成与追问流程」小节的存储与发送描述同步（含追问每轮重新取材）
- [x] 2.5 逐条核对本 change 后 README 的每个「已实现」声明都有已归档规格出处（对照 `specs/project-readme/spec.md` delta 的场景清单）

## 3. 方向文档历史标注（只加横幅，不改写历史正文）

- [x] 3.1 `方向/第一版方向共识-2026-07-01.md` 第 6 节顶部加「2026-07 历史基线」说明（现行状态以 `openspec/specs/` 为准）
- [x] 3.2 `方向/行动计划-2026-08-27.md`：状态注释（第 8 行区域）与事项 2（第 52 行区域）更新至阶段 4 / 5 A 已归档；把指向不存在的活跃 change 路径改为对应 archive 路径
- [x] 3.3 `方向/让AI看见作品-阶段0整体设计-2026-09-09.md` 加文档头「截至 2026-09-09 快照」注记（现行行为由阶段 2–5 A 归档规格定义）

## 4. 架构图与文档地图

- [x] 4.1 `docs/diagrams/project-structure-v2.html` 顶部加历史快照横幅（含日期与「现行结构见 `openspec/specs/`」指针）
- [x] 4.2 `docs/diagrams/README.md` 加状态注记：v2 无 `.drawio` 源、HTML 为可读权威版本、PNG 为导出快照（满足 `workspace-artifact-governance` 源/导出物关系要求）
- [x] 4.3 `AGENTS.md` 文档地图表加《方向/全量地基审计-2026-09-14.md》一行
- [x] 4.4 `方向/全量地基审计-2026-09-14.md` 第八节队列第 1 项状态更新为指向本 change

## 5. 验证与收尾

- [x] 5.1 `npm run check` 全量通过（类型检查、规范、前端测试、可靠性测试、构建、Rust 测试）
- [x] 5.2 OpenSpec 校验通过（change 结构与 specs delta 无错误）
- [x] 5.3 按项目规范提交（加载 git-master skill：查状态、精确暂存、中文提交信息）；后续任何扩大发送面的 change 须同步 `llm-configuration` 告知要求（归档时提醒）
