## 1. DOM 契约与测试夹具

- [x] 1.1 为 AI 面板完整节点集合定义 `AiPanelDom` 类型和集中解析入口
- [x] 1.2 增加完整契约组装、缺失节点错误和局部节点归属测试
- [x] 1.3 迁移或抽取可复用的 AI 面板 DOM fixture，保持现有断言语义

## 2. 生产调用方迁移

- [x] 2.1 让 `getAppDom()` 组装并返回 AI 面板显式契约
- [x] 2.2 迁移 `setupAiPanel` 使用契约节点，删除内部全局查询
- [x] 2.3 检查 bootstrap 和 `AppDom` 调用方，保持现有交互及 AI 零写回边界

## 3. 回归验证与交付

- [x] 3.1 运行 AI 面板 DOM、状态、请求、选区入口和编辑器定向测试
- [x] 3.2 运行完整前端测试、类型检查、lint 和构建
- [x] 3.3 运行 OpenSpec 严格验证，更新路线图和 bug 台账并归档 change
