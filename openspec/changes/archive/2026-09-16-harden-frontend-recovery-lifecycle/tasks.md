## 1. 固定恢复失败语义

- [x] 1.1 在 `ai-session-transport` 测试中分别复现 start、replay history、replay done 返回 `{ ok: false }`，断言恢复拒绝、后续步骤停止、半成品会话尽力结束且后续发送重新启动会话
- [x] 1.2 修改 `replaySession`，逐步检查三个业务结果，只在全部成功后登记会话，并在失败时保留原始错误、尽力清理已启动会话
- [x] 1.3 补充 AI 编排层回归测试，确认传输层业务失败进入 `failRecovery` 而不会标记恢复完成

## 2. 恢复作品重开交互

- [x] 2.1 在编辑器测试中复现 `unload → showProject` 后保存快捷键与右键菜单失效，并断言一次操作只执行一次
- [x] 2.2 收敛编辑器交互模块重建入口，让每次建立作品视图时完整重建查找、工具栏、链接弹层、右键菜单与快捷键模块
- [x] 2.3 运行编辑器相关测试，确认卸载释放、重开恢复及重复建立不产生重复监听

## 3. 补齐会话事件路由生命周期

- [x] 3.1 增加传输层事件路由测试，覆盖重复安装、注册完成后销毁、注册完成前销毁、销毁后重装和旧周期迟到结果
- [x] 3.2 为 `AiSessionTransport` 实现幂等、可重装的事件路由销毁入口，保存并正确释放两个异步 Tauri 退订函数
- [x] 3.3 更新所有传输层测试桩与调用方类型，保持单例和注入实现契约一致

## 4. 统一销毁前端控制器

- [x] 4.1 增加 AI 功能控制器测试，覆盖 DOM 监听、状态订阅、传输订阅与 AI 面板控制器的释放，以及重复销毁和迟到回调不再写状态
- [x] 4.2 为 `AiFeatureController` 实现幂等 `destroy()`，集中释放选择入口、AI 面板、DOM 监听、传输订阅、在途请求和事件路由
- [x] 4.3 调整窗口关闭协调与应用入口测试，确保无未保存内容或确认关闭时先统一销毁前端控制器再销毁窗口，取消关闭时不销毁
- [x] 4.4 在应用入口接入统一前端清理函数，并保持返回欢迎页只结束作品级状态、不销毁应用级控制器

## 5. 验证与审计闭环

- [x] 5.1 运行针对性前端测试、完整前端测试、类型检查与构建，修复本变更引入的回归
- [x] 5.2 运行 `openspec validate --all --strict`，确认全部规格严格通过
- [x] 5.3 更新全量地基审计中的 P1-1、P1-8、P2-11、队列 6 状态、处理进度与最新测试基线
