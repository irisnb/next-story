## 1. 依赖与纯文本适配边界

- [x] 1.1 在 `package.json` 与锁文件中加入原型已验证的 Tiptap 2.27.x / ProseMirror 最小生产依赖，只启用文档、段落、文本和历史能力，并确认未引入 Tiptap Pro、Cloud 或富文本扩展。
- [x] 1.2 为纯文本导入导出先补充失败测试，覆盖空字符串、普通换行、开头和结尾空行、连续空行、中文、标点、emoji 及 20 万字往返一致性。
- [x] 1.3 新建单一 Tiptap 纯文本适配模块，实现实例创建与销毁、完整纯文本初始化与读取、编辑订阅、焦点、选区数据和焦点端坐标接口，使 1.2 的测试通过。
- [x] 1.4 补充外部 HTML/富文本粘贴测试并实现纯文本粘贴规则，确认普通粘贴和粘贴为纯文本只保留可见文字与换行，不保留标题、字体、颜色、列表或其它格式。

## 2. 双本子生产编辑器迁移

- [x] 2.1 更新 `index.html` 与 `src/dom.ts` 的编辑器挂载元素和类型，移除生产控制器对两个 `HTMLTextAreaElement` 的要求，但保持草稿本、正文本标签和现有界面命名不变。
- [x] 2.2 先更新 `tests/editor.test.ts` 等编辑器测试夹具，覆盖两个独立 Tiptap 实例、标签切换保留未保存文字、各自历史隔离、初始化不制造脏状态及旧实例销毁后不再生效。
- [x] 2.3 修改 `src/editor.ts`，为 `draft` / `main` 创建两个独立编辑器实例，切换时只切换可见与可交互状态，并在作品卸载、替换或控制器销毁时解除监听和销毁实例。
- [x] 2.4 将每次用户文字事务投影为两个完整纯文本字符串并继续接入 `EditorSaveState.setCurrent`，保持手动保存、单并发保存、保存失败和离开协调接口不变。
- [x] 2.5 扩充 `tests/editor-save-state.test.ts`、`tests/leave-safety.test.ts` 与相关离开流程测试，证明保存期间继续输入不会被本次快照标记为已保存，取消离开和保存失败仍保留两个本子的当前文字。
- [x] 2.6 调整生产编辑器样式，使 Tiptap 写作区保持当前桌面布局、滚动边界、聚焦表现和草稿本/正文本切换可用，不新增富文本工具栏或格式控件。

## 3. 选区快照与 AI 入口迁移

- [x] 3.1 先改写 `tests/selection-adapter.test.ts`，用编辑器适配数据替代 textarea 夹具，并覆盖正向/反向选区、中文、emoji、换行、空段落和跨段选区的 UTF-16 切片恒等式。
- [x] 3.2 修改 `src/selection-adapter.ts`，从 ProseMirror 有序范围和选区头部生成与控件解耦的 `SelectionSnapshot`，保持 `draft` / `main`、原样 `selectedText`、`start <= end` 和冻结复制语义。
- [x] 3.3 先改写 `tests/selection-entry.test.ts` 与坐标测试，覆盖内核焦点端坐标、同帧事件合并、单次坐标读取复用、滚动可见性、窗口边界、菜单打开后锚点稳定和上下文失效隐藏。
- [x] 3.4 修改 `src/selection-entry.ts`，让活选区生命周期、入口显示和两个开启动作使用当前 Tiptap 实例，同时保持点击后隐藏旧入口及 AI 请求期间继续写作的现有行为。
- [x] 3.5 用 ProseMirror `coordsAtPos` 等内核坐标能力替换 `src/caret-coordinates.ts` 的 textarea 镜像生产路径，确保入口定位不复制完整本子正文，并删除已无调用方的 textarea 专属实现。
- [x] 3.6 运行并扩充 AI 首次请求、思维扩展、重试和追问测试，证明点击后编辑、改变选区或切换本子不会改变已冻结快照，AI 面板仍无任何写入两个本子的入口。

## 4. 项目兼容与自动化检查

- [x] 4.1 增加打开既有 `.txt` 项目、编辑、保存、关闭和重新打开的集成测试，逐项断言两个本子的可见字符、顺序、空行和换行与成功保存快照一致。
- [x] 4.2 检查 `src/project-api.ts`、`src/types.ts` 和 Rust 项目模块，确认打开与保存仍只传递 `draft` / `main` 两个完整纯文本字符串，且作品目录未新增 ProseMirror JSON、HTML 或结构化编辑器文件；若契约无须修改则不产生无关改动。
- [x] 4.3 运行 `npm run typecheck` 和 `npm run test:frontend`，修复本 change 引入的类型或测试失败，并记录任何与本 change 无关的既有失败。
- [x] 4.4 运行 `npm run build` 和 `npm run test:rust`，确认前端生产构建及既有项目文件事务测试通过。
- [x] 4.5 检查生产依赖树与分发构建，确认所用 Tiptap/ProseMirror 核心依赖许可符合已验证的宽松许可范围，且 `npm run tauri:build` 能生成 Windows 分发产物。

## 5. Windows 桌面真机验收

- [x] 5.1 在 Windows Tauri 2 / WebView2 中用微软拼音实敲验证拼音组合、候选选择、长句转换、组合中退格、上屏后退格、撤销重做及组合期间切到 AI 面板再返回，记录无丢字、重复或乱序的结果。
- [x] 5.2 真机验证鼠标和键盘正反向选区、中文/emoji/换行跨段选区、复制、剪切、网页富文本粘贴和纯文本粘贴，确认冻结原文和剪贴板文字准确且未带入格式。
- [x] 5.3 真机验证草稿本/正文本切换、两个本子分别撤销重做、手动保存、保存期间继续输入、打开另一作品、返回欢迎页和关闭窗口的未保存保护。
- [x] 5.4 真机验证 AI 入口在滚动、窗口尺寸变化、边角选区、面板展开收起和不同选区方向下的位置与生命周期，并确认及时召唤、思维扩展和追问继续锚定点击时快照。
- [x] 5.5 用至少 20 万汉字的本子测量连续输入延迟 p95 和载入后的进程增量内存，确认分别不高于 50ms 和 300MB，并验证选区、滚动、撤销重做、保存重开期间无文字损坏或选区漂移。
- [x] 5.6 汇总自动化与真机证据，逐条对照 `production-editor-kernel`、`writing-notebooks` 和 `selection-ai-invocation` 增量规格；只有全部满足且未开放富文本、结构化存储或 AI 写回时，才将本 change 标记为可归档。
