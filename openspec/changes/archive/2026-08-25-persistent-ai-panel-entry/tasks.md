## 1. 请求协议与提示词

- [x] 1.1 为直接提问定义独立的首轮请求载荷，支持必填问题和可选选区，并保留现有选区召唤请求兼容性
- [x] 1.2 更新 Rust 请求校验与提示词组装，使无选区问题可发送、问题与选区材料清晰区分、空问题被拒绝
- [x] 1.3 为直接提问请求和提示词边界补充前后端单元测试

## 2. 面板状态与选区生命周期

- [x] 2.1 扩展现有 AI 面板状态 facade/reducer，保存直接提问草稿、待附带选区和发送状态，并保持订阅通知规则
- [x] 2.2 接入编辑器选区快照：有意义的新选区替换旧待附带选区，清除选区移除待附带材料，发送时冻结问题和选区
- [x] 2.3 将直接提问状态接入现有作品/文档切换清理和迟到结果隔离
- [x] 2.4 为草稿、选区替换/清除、发送冻结、收起恢复和生命周期清理补充状态测试

## 3. 现有 AI 面板入口与渲染

- [x] 3.1 在现有 AI 面板 DOM 契约和页面结构中加入直接提问输入、提交控件、选区提示和移除控件，不创建第二面板
- [x] 3.2 扩展纯 view-model 与 DOM 适配器，呈现空闲输入、已附带选区、加载、错误和临时回应状态
- [x] 3.3 将面板 action 接入直接提问提交与选区移除，并保持现有开合、选区召唤、思维扩展和追问行为
- [x] 3.4 保持响应式并排/收起布局，验证面板重新展开时恢复已有临时状态和未发送输入
- [x] 3.5 补充 view-model 直接测试与 DOM 回归测试，覆盖无选区、有选区、替换、清除、提交禁用和错误恢复

## 4. 验证与边界检查

- [x] 4.1 运行相关前端类型检查、单元测试、DOM 测试和 Rust 测试
- [x] 4.2 验证 AI 输出没有作品文档写入、插入、替换、删除或应用路径
- [x] 4.3 验证实现未引入 Agent 循环、自动上下文读取、检索、摘要、记忆、后台预加载或多对话历史

## 5. 统一临时对话（三个首轮入口共享）

- [x] 5.1 让直接提问首轮成功后进入统一临时对话（锚点 + 完整轮次），不再作为独立状态
- [x] 5.2 让 AI 及时召唤、思维扩展首轮成功后复用同一对话结构，保持现有入口语义
- [x] 5.3 每轮请求携带首轮冻结选区与此前完整问答；首轮选区作为锚点，后续编辑器选区变化不替换原材料
- [x] 5.4 收起面板保留统一对话与未发送输入；切换文档或作品、关闭应用时清空
- [x] 5.5 同一时刻只允许一轮请求；失败保留追问供重试；迟到结果隔离
- [x] 5.6 为统一对话补充状态、view-model、DOM 与编排测试（直接提问追问、完整问答载荷、锚点冻结、收起恢复、生命周期清理）

## 6. 验证与边界检查（统一对话）

- [x] 6.1 运行相关前端类型检查、单元测试、DOM 测试和 Rust 测试
- [x] 6.2 验证 AI 输出没有作品文档写入、插入、替换、删除或应用路径
- [x] 6.3 验证实现未引入 Agent 循环、自动上下文读取、检索、摘要、记忆、后台预加载、会话列表或持久化历史
