## 1. 规格与数据契约

- [x] 1.1 在 automatic-story-context 规格中定义常规轮次现场材料（关注文档已保存正文/未保存快照）、允许目录投影、跨文档命中片段、来源版本与限制状态的数据契约。
- [x] 1.2 扩展讨论关注文档显式切换、材料出处保存与旧会话权限语义的规格，与 controlled-story-read-visibility 的永久只读与旧出处脱敏语义对齐。
- [x] 1.3 明确常规入口与及时召唤入口的取材分流，并补充迁移/兼容测试。
- [x] 1.4 定义材料生命周期状态模型（prepared / sent / omitted / rejected / failed）与检索结果状态（not_found / no_query_terms），并确认现有 provenance `entered_model_context` 与 sent 的对应关系。

## 2. Rust/Tauri 受控取材

- [x] 2.1 实现按关注文档读取当前保存稿或经校验的当前编辑器未保存快照；复用既有 `projectPath`/`documentId`/`documentVersion`/`bodySnapshot` 与 `canonicalNotebookJson` 契约，不另造第二套 ID。
- [x] 2.2 验证无选区直接提问仍可提供关注文档快照；非当前编辑器关注文档只用已保存正文；快照校验失败拒绝且不静默回退到磁盘稿。
- [x] 2.3 实现允许读取目录投影，过滤回收站与不可见内容；验证隐藏文档名称/ID/路径/正文不出现在投影中，仅保留匿名数量。
- [x] 2.4 实现候选词确定性提取：NFKC + 英文小写 + 连续空白折叠并保留到原文位置映射；中文连续片段原样抽取、拉丁字母数字连续片段；至少 2 Unicode 字符、去重按首次出现至多 8 词；不调用模型、不从作品反向抽词。
- [x] 2.5 实现跨文档字面检索：同作品、非回收站、当前可见、已保存正文、排除关注文档；正文搜索（标题不冒充正文）；命中前后各 120 Unicode 字符、重叠合并、至多 5 文档 10 片段、稳定排序。
- [x] 2.6 保证单轮材料冻结、版本一致、权限失败关闭；冻结时点与排队前权限复验，后续编辑不改变该轮。
- [x] 2.7 负向测试：跨作品检索、回收站文档、隐藏文档、游离节点、错误版本、非法范围、伪造快照、正文零写回、标题命中不返回正文。

## 3. 常规讨论接入

- [x] 3.1 将现场材料、目录投影和跨文档候选片段接入常规首轮与追问的 DSH task；验证首轮与追问都带本轮材料而非历史材料。
- [x] 3.2 保持及时召唤首轮及追问不经过完整常规取材流程；负向验证召唤请求不包含现场材料/目录/检索片段。
- [x] 3.3 实现显式切换关注文档及清晰用户提示；验证查看其他文档不改变关注对象、切换下一轮生效。
- [x] 3.4 防止多窗口讨论之间串用关注文档、快照或材料；负向验证并发讨论各自独立。
- [x] 3.5 超限交给既有 DSH/provider 处理，不前置猜测或计算模型上下文容量；验证材料总量只受检索输出硬上限约束、不手工截断历史、不做选择器/自动缩减重试/文件上传；仅明确超限错误（`request_too_large`）如实记录失败且不伪称未发送。
- [x] 3.6 不新增模型容量表、不凭固定数值估计 token 预算；确认阶段五不计算上下文容量，交由既有框架压缩与 provider 处理。

## 4. 记录与界面说明

- [x] 4.1 保存每轮材料出处（关注文档、未保存快照、目录、检索来源文档/版本/位置/匹配词、限制状态），只存最小元数据不复制正文。
- [x] 4.2 实现材料状态诚实记录：`entered_model_context` 只表示「想发送 / 已组装进请求」。provider 发送回执已落地为 `message_sent` 驱动事件（本轮首次观测到 provider 侧回应证据时发出一次、先于终态；取消/失败于证据之前不发；仅组装未到 provider 不发）；宿主折算为轮级 `sent_confirmed`（只有收到回执才标记，未确认不伪造），「本次参考了什么」区分显示「已确认送达」与「已组装、送达未确认」。
- [x] 4.3 增加轻量"本次参考了什么"查看入口，展示实际材料与限制；负向验证不列出未读取内容为已知内容、not_found / no_query_terms 不作为正文展示。
- [x] 4.4 权限变化后旧会话历史和出处仍可查看（旧出处脱敏、不泄露隐藏文档身份），新请求重新授权；受限讨论永久只读。

## 5. 验证与收口

- [x] 5.1 添加关注文档、未保存快照、显式切换、下一轮生效和追问版本更新测试。
- [x] 5.2 添加候选词提取、规范化位置映射（含 NFKC 兼容字符）、跨文档检索、去重排序、权限过滤、回收站、未命中和材料超限测试；验证 8 候选词/5 文档/10 片段/命中前后 120 字符为检索输出硬上限而非模型上下文上限。
- [x] 5.3 添加材料状态（prepared/accepted/omitted/rejected/failed/unknown）测试；验证回执能力未落地不标 sent、`entered_model_context` 只表示想发送、未知状态不伪造 sent。
- [x] 5.4 添加及时召唤隔离、多窗口隔离、旧会话查看脱敏和新请求权限测试。
- [x] 5.5 运行完整 Rust、前端、集成链路和规格校验，记录真实模型材料证据；验证不注册 Agent 工具、文件上传或语义检索；确认检索候选词与片段上限为内部初始值，非产品效果保证。
  - 验证记录（2026-09-14）：Rust lib 198 通过 / export 12 / llm_config 31 / project 33，前端 745 通过 + typecheck + lint 干净；真实链路驱动级回归（智谱 coding 接口 + `glm-5.3-flash`）12 项全过：`message_sent` 回执恰一次且先于终态、流式 delta、多轮上下文、历史重放、取消终态与取消后追问、干净退出、stderr 无 key 泄漏。材料组装链路由 Rust 集成测试（假驱动）覆盖；真实模型 + 材料组装的应用级 E2E 按用户选择以驱动级验收替代，未运行完整 Tauri 应用实测。
  - 真实链路发现并修复传输缺陷：DSH 默认 `max_tokens=256000` 被严格校验端点拒绝（智谱接口允许 [1,131072]），`driver.mjs` 新增 `--max-tokens` 参数（默认 131072），宿主可按端点覆盖。
