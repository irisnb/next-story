# Tasks: 多窗口与及时召唤快车道（阶段 3）

## 1. 流式与终态按讨论身份路由

- [x] 1.1 `ai-session-transport` 流式订阅回调携带讨论身份（`conversationId` 与消息身份），不再只传文本
- [x] 1.2 `ai-feature` 按讨论身份把增量写入对应讨论，替换「统一写入当前活动讨论」
- [x] 1.3 reducer `append_stream_text` 改为按讨论身份写入；终态与迟到结果沿用讨论身份隔离
- [x] 1.4 测试：两个讨论并发流式，增量各自归属、不串台
- [x] 1.5 测试：迟到增量与终态不污染其他讨论（含已关闭窗口的讨论）

## 2. 状态结构：窗口集合与草稿归属

- [x] 2.1 `AiPanelState`/reducer 增加窗口结构状态（打开窗口：讨论 id + 停靠/浮动；聚焦窗口）
- [x] 2.2 替代单一 `activeConversationId` 的显示语义；会话列表高亮按聚焦窗口
- [x] 2.3 输入草稿与待附带选区按窗口/讨论归属；编辑器选区附着聚焦窗口的发送
- [x] 2.4 切换作品：关闭全部窗口、清空视图、加载新作品讨论列表；切换文档不清空、不关闭窗口
- [x] 2.5 测试：窗口结构状态迁移、订阅通知与非法迁移不通知

## 3. 取消接线与停止生成

- [x] 3.1 transport 增加按讨论取消的方法，接线既有 `ai_cancel_message` 命令
- [x] 3.2 区分用户停止与超时：用户停止显示「已停止」，已完成内容保留、未完成不产生成功轮次
- [x] 3.3 每窗口提供停止生成动作（生成中或排队中可见）
- [x] 3.4 关闭生成中窗口：先停止该轮再关闭显示；其他讨论不受影响
- [x] 3.5 测试：停止只影响对应讨论；关闭窗口停止对应请求；重开不自动重发

## 4. DOM 契约多实例化与渲染管线

- [x] 4.1 `dom.ts` 契约改为按窗口根节点解析，去除全局 `getElementById` 依赖
- [x] 4.2 `index.html` 提供停靠区容器与窗口模板；原面板静态节点移入模板
- [x] 4.3 每窗口实例化 DOM 契约与渲染管线；`setupAiPanel` 支持多实例创建与销毁（含订阅退订）
- [x] 4.4 显示决策按窗口参数化（以窗口讨论为输入），保持纯派生与单一事实源
- [x] 4.5 测试：多窗口契约组装、缺少节点明确失败、窗口销毁释放订阅

## 5. 窗口管理：停靠/浮动/移动/缩放/并排

- [x] 5.1 新增窗口管理器模块：创建、销毁、聚焦、置顶
- [x] 5.2 停靠区承载多个窗口（纵向排列、超出滚动）；停靠区可收起/展开且不停止生成
- [x] 5.3 窗口可拖出成为浮动窗口：移动、缩放（含最小可用尺寸）、并排对照；可拖回停靠区
- [x] 5.4 窗口头：讨论标题与当前关注文档、停止生成、关闭窗口
- [x] 5.5 关闭窗口只结束显示不删除讨论；重开聚焦已有窗口；删除讨论同时关闭其窗口
- [x] 5.6 布局仅运行期保留：位置尺寸不写入档案、不持久化；重启恢复默认停靠
- [x] 5.7 提供「恢复默认布局」动作，一键回到默认停靠与默认尺寸
- [x] 5.8 测试：窗口生命周期、一讨论至多一窗口、布局不落盘

## 6. 请求调度与快车道

- [x] 6.1 移除作品级首轮单飞门禁；首轮预检按讨论进行
- [x] 6.2 修复全局 `conversationGeneration`：开新讨论/切讨论不使其他讨论的在途预检作废
- [x] 6.3 新增调度器：全局并发上限（可配置）+ 先到先服务队列；排队请求占用其讨论单请求锁
- [x] 6.4 排队状态只在对应窗口呈现（不显示为生成中）；名额释放后按序开始
- [x] 6.5 排队中的请求可被停止或关闭窗口取消
- [x] 6.6 限流与服务能力不足：对应窗口如实提示、可手动重试，不自动重试风暴
- [x] 6.7 测试：不同讨论首轮并发、达到上限排队、名额释放顺序、排队取消

## 7. 崩溃恢复覆盖所有打开窗口

- [x] 7.1 驱动进程丢失后对所有打开窗口的讨论执行重放恢复
- [x] 7.2 各窗口显示恢复中状态；完成后恢复追问；未打开讨论保持档案待重开
- [x] 7.3 测试：多窗口分别恢复、在途请求不伪装成功

## 8. 会话列表与新建对话适配

- [x] 8.1 会话列表重开：已打开则聚焦其窗口，否则新建停靠窗口
- [x] 8.2 「新建对话」打开新讨论窗口（聚焦窗口已是空窗口时复用），保留旧窗口
- [x] 8.3 直接提问首轮与及时召唤首轮在新窗口打开，不替换已有窗口
- [x] 8.4 测试：重开/新建/召唤的窗口行为与旧讨论保留

## 9. 会话列表重做

- [x] 9.1 `conversation_store` 支持可选自定义标题与置顶标记字段（读写、缺失按默认、向后兼容）
- [x] 9.2 前端档案模块支持重命名与置顶的保存与更新
- [x] 9.3 列表改用 `updated_at` 排序，实现「置顶 / 今天 / 昨天 / 本周 / 上周 / 本月 / 上月 / 更早」分组、相对时间与条目间轻分隔线
- [x] 9.4 紧凑行呈现：两行标题省略、关注文档与状态、当前讨论标记；状态词与窗口一致
- [x] 9.5 重命名：就地编辑、回车保存、Esc 取消
- [x] 9.6 置顶 / 取消置顶
- [x] 9.7 删除确认与撤销提示
- [x] 9.8 按标题与关注文档名过滤；空结果与初始空状态
- [x] 9.9 列表覆盖停靠区并可收起；编辑区保持可见
- [x] 9.10 入口常驻：停靠区头「会话列表 / 新建对话 / 收起」不按讨论数量显隐；0 条时列表显示空状态
- [x] 9.11 测试：分组/排序/重命名/置顶/删除撤销/过滤/入口常驻

## 10. 真实链路验证与等待基线

- [x] 10.1 增加轻量等待计时采集：提交、首次真实模型回应、完成、应用内排队时长
- [x] 10.2 核对真实 DSH 事件字段与消息身份（阶段 1 遗留项）
- [x] 10.3 验证并发双讨论：同时生成、增量与终态各自归属
- [x] 10.4 验证快车道：常规生成进行中召唤独立发起
- [x] 10.5 验证停止隔离：停止/关闭只影响对应讨论
- [x] 10.6 验证队列行为：超出上限排队与名额释放顺序
- [x] 10.7 驱动丢失恢复：未做用户实测（用户判定场景不现实、拒绝）；保留实现与单元测试覆盖，结果记录在验证记录
- [x] 10.8 并发上限暂定 2（可配置常量）；等待分项数据本轮未采集（需应用内开发者工具导出；仪器保留，见 `validation.md`）
- [x] 10.9 真实模型验证需要用户在场参与并确认观察结果

## 11. 视觉落地（按 ui-design 设计稿）

- [x] 11.1 按 `ui-design/README.md`、`mockup.html`、`window-anatomy.html` 实现窗口视觉（窗口头、四态、停靠/浮动、活动/非活动）
- [x] 11.2 按 `conversation-list-detail.html` 实现会话列表视觉
- [x] 11.3 视觉走查：与设计稿一致，不引入体系外散配色；编辑区保持最重边框
- [x] 11.4 按 `ui-design/interaction-rules.md` 核对交互规则落地（拖动中断、按钮状态表、入口常驻）

## 12. 文档同步与回归

- [x] 12.1 更新 `AGENTS.md` 当前实现摘要（多窗口、快车道、停止、调度、恢复、会话列表）
- [x] 12.2 更新 `README` 已实现/未实现表述
- [x] 12.3 同步方向文档阶段状态（行动计划、开发指引的阶段表）
- [x] 12.4 `npm run check`、`npm run test:rust`、前端测试与离线验证回归全绿（最终回归：前端 628 + 可靠性 89 + Rust 207 + 构建全绿）
