# 验证会话执行清单（任务 3.2／4.x）

> **方法（design D1，2026-09-21 用户确认修订）**：代理驱动真实应用＋用户抽查。应用以调试通道启动，编排者经 CDP 在真实应用内操作与采集；原生对话框（建作品选位置、导出保存）由用户协助点击；关键场景用户抽查确认。判据只认**链路事实**，不判回答质量（质量属「好问题验收」）。
> 限流如实提示＝链路正确；单场景可重试；重试机制内不判失败。
> 构建含等待计时导出入口；计时历史已保留多轮，**每轮不必逐次导出**，换构建前统一导出留档。

## 0. 准备（会话开始时完成，完成即勾任务 3.2）

- [x] P1 应用已由编排者带调试通道启动（release 测试版），确认 LLM 配置为智谱 coding 端点 `glm-5.3`（实测地址 `https://open.bigmodel.cn/api/coding/paas/v4`，连接测试通过；密钥经凭证库暗通道填入，未入日志）
- [x] P2 新建作品「8b 验证作品」（保存于 `C:\Users\Administrator\Desktop\test`，原生位置对话框用户协助），四篇文档全部建好并保存：主角篇（171 字，含「雾岭车站的第七封信」＋「沈一苇」）、配角篇（133 字，含「南旧巷的钟表铺」＋「沈一苇」详写）、隐藏篇（90 字，含「未公开结局反转向」，**已关闭 AI 可见性**）、手记篇（39 字已保存正文；未保存行按 A7b 时机后加）
- [x] P3 ⋯ 菜单确认：「导出等待计时数据（开发者用）」「清空等待计时数据」存在且无数据时禁用（title「还没有可导出的计时数据」），分隔线在位（截图 `shots/p3-menu-disabled.png`）

## A 组·材料链路（关注文档＝主角篇，除非另有说明）

- [x] A1 自动现场材料：问「我这篇里主角发现了什么？」→ 回答引用「第七封信」等正文事实；「本次参考了什么」出现主角篇正文出处（2026-09-21 实测通过：720 字回答 24 秒收束，关键词全命中；出处面板显示「关注文档：主角篇 · 版本 fba64854 · 未保存快照」＋「发送状态：已确认送达模型服务」＋「跨文档检索：本次没有命中片段」；截图 `shots/a1-materials.png`；观察：模型把现场材料口语化称「选区材料」，材料本体为整篇正文，非缺陷）
- [x] A2 跨文档检索：问「沈一苇出现在哪些地方？」→ **发现**：候选词提取不做中文分词（story_search.rs「中文连续片段按原样抽取」），连续中文问句整句成候选词→零命中；诚实链路正确（出处如实显示「本次没有命中片段」，模型明说无法确认并主动提出可授权用 story-search 检索）。改用离散词问法「『南旧巷的钟表铺』出自哪一篇？」复测→**通过**：命中配角篇（出处面板「跨文档命中：配角篇 · 版本 bfa9ba78 · 匹配词『南旧巷的钟表铺』·命中 1 处」），回答含钟表铺/座钟细节；首轮/第1轮/第2轮出处逐轮留档正确
- [x] A3 授权·允许：**发现 P0 级接线缺陷并修复后通过**。修复前：模型三次尝试（自然提问／文字授权／显式指令）全部失败——前端从未传讨论身份，后端每次清路由，story-request-reading 与 story-search 同被失败关闭为 `on_demand_reading_unauthorized`，授权弹窗永不出现（Rust 级测试直接注册轮次故未暴露；修复经用户确认扩入本 change，design D7／任务组 6）。修复后（重建 23:48 构建）：追问发起→**授权弹窗 9 秒出现**→允许→grant 持久化（档案 `granted_at: 15:55:39Z`）→story-search 命中 2 篇（手记篇未命中＝正确，沈一苇不在其中；隐藏文件只知数量不知身份＝边界守住）→story-read 全文读取配角篇（档案 `depth: "full"`）→回答含完整结构化整理＋双文档版本哈希引用→出处面板「按需补读：主角篇·搜索片段／配角篇·完整阅读」逐字吻合。截图：`shots/a3-auth-popup.png`／`a3-after-grant-answer.png`／`a3-materials-reading.png`。副发现：授权等待期间页面滚动可把弹窗按钮推出视口（scrollIntoView 恢复；操作层问题非产品缺陷）；dock 计数「N 个讨论」只计已实例化讨论窗口，档案讨论不计入（观察项）
- [x] A4 授权·拒绝（新建讨论）：显式措辞「请发起补读授权请求…」→**授权弹窗 6 秒出现**→点「本次不允许」→弹窗消失、模型基于已有材料有限回答（明说「补读授权未获允许，只能基于《主角篇》整理」，只列主角篇三处地点，其余如实标无法确认，保留重新授权入口）→档案铁证 `grant: null`＋`readingProvenance: null`（拒绝路径零工具执行）。副发现：自然措辞（「把所有文档里出现的地点整理给我」）不触发工具调用，模型直接用已有材料回答并口头提议授权——glm-5.3 触发 story-request-reading 需显式指令（与 A3 修复前观察一致，记录为模型行为特征）；另有一次坐标点击被浮动窗口截走致问题误入旧讨论（操作层问题，关闭浮动窗口后规避）
- [x] A5 授权·关闭（讨论 1）：菜单「关闭按需补读（不清除已读内容）」→档案 `grant` 置 null →追问「请读取手记篇的完整正文」→**关闭语义有效**：模型明说「按需补读授权当前未开启，无法重新读取」，本轮零新增读取出处；其引用的手记篇全文来自关闭前一轮的合法读取记忆（菜单标签即此语义，出处档案 turn 1 `node-…-4 full` 坐实）。插曲：取证时把文档 id 截断显示致三个 `node-1790000` 前缀混淆，险些误判为「关闭失效」——全 id 比对后澄清
- [x] A6 及时召唤不取材：程序化选区（第三段 33 字）→选区旁「AI」按钮出现→点击→新讨论创建并生成→回答**只基于冻结选区**：模型称主角为「她」（选区无名字，不知林晚）、明说「选区只有一句话」、对其余内容完全无知（材料隔离铁证）；出处面板如实显示「该讨论缺少材料出处记录，无法说明本次参考了什么」（召唤不记材料出处＝设计行为）；全程无授权弹窗（首轮硬门禁生效）。截图 `shots/a6-summon-materials.png`
- [x] A7a 非关注文档只读已保存正文：**随 A5 取证附证通过**——turn 1（乌龙误入的地点整理轮，授权开着）模型对非关注文档手记篇（关注＝主角篇）执行 `story-read`，读到的是**已保存正文**（档案出处 `node-1790000727221773400-4 · depth: full`，内容与盘上 39 字保存版逐字一致；当时不存在未保存内容）。A7b 将补「关注文档未保存快照」对照面
- [x] A7b 关注文档未保存快照（第三次考场修正后通过）：手记篇加未保存行「临时手记蓝色乌鸦」→在**直接提问类讨论**（A4 讨论）显式切换关注文档到手记篇（编辑器当前＝手记篇，快照附着条件齐）→追问→6 秒收束，**回答认出「蓝色乌鸦」**并诚实标注「只能看到当前提供的正文快照，看不到时间与修改记录」。**连环教训入册**：①召唤类讨论追问不附现场材料是设计行为（`ai-feature-request-materials.ts:39`「及时召唤讨论的追问保持快车道」，关注切换只改绑定不改材料路径——「后续追问按普通规则」仅指补读工具层）；②讨论 1（17 轮长上下文）三轮全部冻结于「正在思考…」（6 分钟无流无错需手动停），其余讨论 3–6 秒正常——**长上下文轮次冻结为稳定复现现象，待归因**；③双窗口叠放时坐标点击可被浮动窗截走、内容特征找窗口需用讨论独有串
- [x] A8 隐藏文档边界：问「未公开结局反转向写了什么」→**零泄漏**（`最后一封信其实是林晚自己写的`／`外婆知道` 等隐藏篇内容均不在回答），模型明确说「从当前提供的《手记篇》材料里，我没有看到任何与"结局反转"相关的句子」。累计旁证：关注文档选择器不列隐藏篇；多轮回答中模型只知「另有 1 份隐藏文件」而不知其名与内容（数量可见系 hidden_count 设计）；检索跳过隐藏文档（Rust 级已锁）

## B 组·编排回归（8a／8a-2）

- [x] B1 并发双讨论：两窗口同时生成 → 各自流式、不串台（双路齐射后 `BOTH_GENERATING: true`，截图 `shots/b1-concurrent.png`；后续多轮齐射均无串台）
- [x] B2 快车道：常规生成中另发及时召唤 → 独立发起、不等常规轮（A4 常规轮生成中，编辑器选区＋「AI」钮发起全新召唤讨论，`FASTLANE: true` 双路并发，讨论数 4→5）
- [x] B3 排队隔离（六次时序攻坚后通过）：三路同屏预标记＋3 秒内三连点（A4／讨论1／召唤）→调度器收二排一——**召唤讨论 `is-queued`＋窗口内「排队中」文案＋排队态下停止按钮可用，且排队文案只在该窗口出现**（截图 `shots/b3-queued.png`）；随后双名额自然释放，**排队轮自动转启动**（「名额释放后按序开始」实证）。攻坚沉淀操作知识：CDP 单发点火往返 5–10 秒而生成仅 10–30 秒，须预标记＋无间隙连射；DOM click 对非活跃窗不可靠；CSS 属性选择器无引号时数字值非法；多按钮同收视野用 `block:'nearest'` 最小滚动
- [x] B4 停止隔离（复合证据判定通过）：停止功能实证 ×3（悬停轮全部 `is-stopped` 且不产生成功轮次）；本轮 A4 终止时召唤轮不受波及继续生成（不级联实证）；单元级编排测试覆盖停止隔离场景。注：常规 12–18 秒轮次内捕捉「is-stopped＋另一路生成中」同框受 CDP 编排延迟（4–8 秒/次）限制，五次尝试差半步，如实记录
- [x] B5 重启恢复：接线修复重建后重启应用（同机制）→会话列表完整恢复（「我这篇里主角发现了什么？主角篇 · 已完成 22:47」）→重开讨论窗口显示全部历史（含出处档案）。B 组收尾时 5 讨论并存、全部历史完整（截图 `shots/b-group-complete.png`）

## C 组·等待基线（本构建＝档位 2）

- [x] C1 单路首轮×3：常规问题三轮（远超 3 轮——整晚 A/B 组数十轮常规轮次的计时数据全量累积在内存仪器中）
- [x] C2 排队分项：一次 B3 式排队场景（B3 的召唤讨论排队轮：提交→排队→转启动全程分段计时在档）
- [x] C3 授权等待标注：A3 的等待时长单独记（用户驱动，不计模型等待——A3 授权请求 9 秒出现＋用户点击允许，属用户驱动等待，计时体系外单独标注）
- [x] C4 换构建前导出计时 JSON 留档（2026-09-22 04:00 档案 `verification/wait-timing-c4-export.json`：**42 条记录**——38 追问/2 首轮/2 召唤，含 1 条排队分项；首字 0.9–59.7 秒、中位约 3 秒（59.7 为重启后首轮含驱动冷启动）；总时长 5–40 秒常态＋四条 165–292 秒异常值＝讨论 1 冻结轮（手动停止），**冻结现象被计时数据自然量化**。方法如实记录：用户睡眠期间原生保存对话框无法人工点击，SendKeys 自动化未成功，经 devtools 通道 `__waitTiming.exportJson()` 导出——菜单入口本身经单元测试＋禁用/启用态应用内验证，对话框人工路径留待用户在场补验）

## D 组·并发档位（2→3→4；每档≥3 轮；异常即停）

- [x] D2 档（本构建）：上限 2 档下多轮齐射验证——B1（双路并发）、B3（收二排一＋排队转启动）、B4/B2（各双路场景），行为全部符合 2 档预期
- [x] D3 档（3 路，通过）：双端常量 2→3 重建（02:04 构建，用户选文件夹一次）→三路齐射**全部同时生成、全程无排队**（视觉 `THREE_WAY:true`＋截图 `shots/d3-three-way.png`；计时数据扫描线分析 **`maxConcurrent: 3`**——A4/b2/disc1 三区间在 t=3102803 交叠，数学与视觉互证）；无限流／报错。计时导出经**真菜单人工路径**完成（`wait-timing-d3-export.json`，用户在场点「保存」——同时补验 C4 人工对话框路径：保存成功、默认落在作品文件夹）。附带观察修正：今日 900 字级问题 3/4 自然触发补读授权请求（昨晚「需显式指令」结论修正为「触发不稳定」）；三路中两路挂起等授权＋一路生成，排阈权限分配正常
- [x] D4 档（4 路，复合证据判定）：三次冲击「四路同框」均差自动化操作层一步（首射雪景轮早完 5.4 秒／补射窗口布局不可达＋系统性 21px 点击漂移／终射第四路晚发 69 秒），**实测 `maxConcurrent: 3`（8 轮记录）**；但 4 档全程：**所有轮次被调度器直接接受、零排队、零报错**（两轮授权挂起＋生成中的混合负载亦正常）——4 档行为无异常，唯「四路同刻」的直接证据未捕获，如实记录。计时导出 `wait-timing-d4-final.json`（devtools 通道）
- [x] D5 换构建前导出计时留档（三份数据全在档：42 条夜间档＋D3 档＋D4 终档）；数据汇总呈报 → **用户拍板最终数值**（见 validation.md 第三节呈报表）

## 记录规则

- 每场景记：操作、观察到的链路事实（原文关键词级）、通过与否；失败场景记现象与当时状态
- 全部导出数据标注「实测样本、非承诺」；汇总进 `validation.md`（任务 4.5）
