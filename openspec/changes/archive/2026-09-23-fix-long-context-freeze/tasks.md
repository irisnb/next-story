# 任务：长上下文轮次冻结的归因与修复

> 依赖顺序：离线归因（1）→ 实弹定向复现（2）→ 修复（3）→ 测试（4）→ 验证归档（5）。
> 实弹组需要智谱配额与部分用户在场场景，提前约时段；离线组零 API 费用。

## 1. 离线归因：源码研读与假设台账（零 API 费）

- [x] 1.1 通读 `dsh-compaction-basic` 与 `dsh-compaction-tool-result-pruner` 源码：压缩触发条件、压缩请求形态（是否一次超长上下文请求）、压缩期间对外协议事件行为，结论入台账
- [x] 1.2 通读 `dsh-agent-loop` 与 `dsh-session-persistence-jsonl`：压缩与工具链期间事件流是否停摆、会话 JSONL 路径与粒度（能否作冻结轮尸检材料），结论入台账
- [x] 1.3 审计 `dsh_driver.rs` 的 `is_suspended` 置位／清除条件全集，核对 180 秒 `REQUEST_TIMEOUT` 对 8b 异常值（165–292 秒）的生效路径——确认冻结轮走入无超时分支的结构洞假设成立与否
- [x] 1.4 核对 sidecar 装配：`cordis.driver.yaml`／`gen-config.mjs` 中压缩参数与 `dsh-llm-retry`／`dsh-timeout` 类插件的实际装配状态与默认值
- [x] 1.5 产出《假设台账》（`verification/attribution.md` 首版）：每条假设附判死条件与最便宜验证手段；用 8b 计时基线（42 条）校准停滞窗口与总时限的分工提案

## 2. 实弹定向复现（少量、按台账打）

> **2026-09-23 排期调整**：静态归因（attribution.md）已把修复形状定死（H1 必修、H4 必配），实弹从「探索」转为「验证」，本组与任务 5.1 真实链路终验合并执行（需智谱配额与用户在场，提前约时段）；判别项见台账第七节。

- [x] 2.1 搭好 8b 形态复现路径（重上下文讨论＋按需补读循环＋全文读取）与取证流程：冻结发生先取计时 JSON／驱动日志／会话 JSONL 三样，再手动停止——经最近列表一步重开 8b 作品与 25 轮重讨论，路径与取证流程验证可用；本会话冻结未发生，「先取证再停止」分支未触发
- [x] 2.2 按台账跑差异化实验（智谱对照 DeepSeek、压缩阈值附近构造、开／关压缩对照——配置暴露时），逐次归档取证包——**前提随归因消解**：压缩对照因 H4 反转失去意义（阈值 80 万 token 从未接近），DeepSeek 差异化属 H2b 可选判别项未执行（H1 已代码级坐实，修复覆盖全部 H2 子变体），如实记录
- [x] 2.3 归因裁决落档：根因归入「我方配置／我方代码／DSH 内部／端点行为」之一（或如实概率排序）＋置信度＋证据索引，更新 `attribution.md`——裁决：我方代码（H1 结构洞，代码级坐实）×外部停滞（H2 触发侧）＋名额烧伤放大（H3）；H4 反转为非缺陷

## 3. 修复实现（形状按 design D3 决策树随裁决落位）

- [x] 3.1 Rust 驱动层拆分等待语义：「等用户」（授权决定，保持无期限）与「等 DSH／端点」（必须有界）分离，收窄现状 `is_suspended` 无超时分支
- [x] 3.2 实现停滞检测：协议事件间隔口径＋静默窗口常量（用 1.5 校准值落地，不设用户设置项）；触发后走既有取消路径回收、复用既有中文错误通道（含可重试指引）
- [x] 3.3 重新校准 `REQUEST_TIMEOUT` 与停滞窗口的分工与数值，裁决理由写入验证记录（REQUEST_TIMEOUT=180s 原职不动，STALL_WINDOW=180s 事件间隔新任，见 validation.md 二节）
- [x] 3.4 按归因裁决实施治本部分：**裁决不修配置**——H4 经权威查证反转（官方 1M≈默认 100 万，差 4.6% 无实质影响）；JSONL 维持不启用；裁决与理由见 validation.md 二节

## 4. 测试与回归

- [x] 4.1 新增 Rust 驱动测试：长静默识别报错且内容保留、授权等待豁免、事件间隙不误判、停滞回收不波及其他讨论、停滞态手动停止仍有效（对齐 spec 增量四场景）——7 项新测试全过
- [x] 4.2 全量回归：Rust 单元＋集成、驱动 13、离线验证 78、前端套件、typecheck／ESLint／生产构建／`cargo check --all-targets`——`npm run check` 全链＋`test:validation` 78/78 全绿（2026-09-23）
- [x] 4.3 核对前端错误呈现：停滞错误经既有通道到达面板的中文文案与可重试入口（沿用既有映射则零改动，验证即可）——`ai-panel-view-model.ts:412-417` error 态直显 message，零前端改动确认

## 5. 验证、仪器沉淀与归档

- [x] 5.1 真实链路终验：原复现路径上或不再冻结、或停滞被诚实识别收场（对照 spec 场景），取证包与结论入 `verification/validation.md`——4 场景全过（2026-09-23，CDP 模拟真实前端操作）：常规轮 8.9 秒／4.2 秒正常收束、授权等待 300 秒零停滞误报、25 轮重讨论压测正常收束、许可释放；冻结本会话未复现（间歇性）＋停滞触发路径未获实弹直接证据，均如实记录（validation.md 第五节）
- [x] 5.2 诚实边界复核：归因结论、修复边界（治本／兜底）、剩余风险三段式写入验证记录；修复边界同步审计文档补充条目与队列第 9 行状态——validation.md 第四／五节三段式在档；审计补充二十二与队列第 9 行已同步
- [x] 5.3 评估归因仪器（长上下文自动驱动＋事件时戳）是否沉淀为可复用工具；若是，按顺带原则评估 8c 评分器三条候选是否并入，裁决与理由记档（不扩权）——裁决：仪器为静态研读＋单元测试，未产出可复用工具，评分器候选不并入（validation.md 二节）
- [x] 5.4 用户确认后走 openspec archive：spec 增量同步主规格，全量严格校验通过——增量已并入 `resident-ai-session`（新增「生成轮次停滞有界并诚实兜底」＋四场景，Purpose 同步）；AGENTS.md「已知未归因问题」条目随归档更新为已修复并新增能力条目
