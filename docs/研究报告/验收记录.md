# 报告验收记录

验收日期：2026-08-08

## HTML

- 通过 Chromium 打开本地 HTML，页面标题为“开源编辑器与剧本解析方案调研”。
- DOM 文本包含 9 个章节：范围与约束、先看结论、总对比、模型差异、项目档案、界面与审美、Next Story 适配、待实测清单、来源附录。
- 关键内容已出现：原生 textarea、CodeMirror 6、fountain-js、Next Story 的稳定要求、中文输入法与选区冻结实验。
- 页面包含 13 行方案对比表、1 个内联 SVG 结构示意图、19 个来源链接。
- 页面没有外部图片元素，也没有控制台错误；报告不依赖远程图片加载。
- UI 评价章节明确区分候选的典型界面气质、剧本写作匹配度和待实测状态，并记录 neobrutalism.dev 只是视觉参考，不是集成证据。
- Chromium 生成的检查截图：`mobile-check.png`（390x844 窄视口）、`desktop-check.png`（1440x1000 桌面视口）。

## 响应式与打印

- CSS 在 900px 以下把目录改为网格、卡片改为单列、档案字段改为单列；桌面和窄视口均完成 Chromium 截图。
- 打印样式隐藏目录、压缩表格、保留背景和内联示意图，并对卡片、档案、图示和提示框设置避免拆页。
- PDF 由同一 HTML 使用 Playwright 安装的 Chromium 1228 命令行打印生成，没有单独维护第二份内容。

## PDF 文件完整性

- 文件存在，大小：689,540 bytes。
- 文件以 `%PDF-` 开头并包含 `%%EOF`。
- PDF 字节结构中发现 9 个 `/Type /Page` 页面对象。
- 系统没有安装 `pdfinfo` 或 `pdftotext`，因此没有声称完成独立文本抽取；关键内容已通过 HTML DOM 验收，PDF 已通过文件结构验收。

## 证据边界

- 版本号、发布时间和部分许可证字段没有在本轮现场锁定，已在证据清单与 HTML 中明确标为“待锁定”。
- 中文 IME、Tauri WebView、长文本性能、复制粘贴和真实解析覆盖仍属于待实测项，报告没有把它们写成已通过。
