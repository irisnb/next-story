## 1. 固定现有显示行为

- [x] 1.1 盘点 `src/ai-panel.ts` 当前处理的全部请求与临时对话显示分支，并把每个分支对应到现有文案、可见区域和操作可用性
- [x] 1.2 新增不依赖 fake DOM 的显示决策测试，覆盖 idle、首次预览或加载、思维扩展、首次成功、首次失败和配置缺失，并确认测试在实现前按预期失败
- [x] 1.3 扩充显示决策测试，覆盖追问加载、追问成功、追问失败、重试与编辑重发，并确认测试在实现前按预期失败

## 2. 建立纯显示决策边界

- [x] 2.1 新增严格类型的 AI 面板 view model 与纯构建函数，只接收只读面板状态和只读临时对话
- [x] 2.2 用穷尽分支实现现有状态到显示结果的映射，不访问 DOM、不调用 action、不保存可变状态，并使显示决策测试通过
- [x] 2.3 检查 view model 不暴露 DOM 节点或 CSS 操作步骤，也不复制 `AiPanelState` 拥有的请求或对话状态

## 3. 收窄 DOM 适配器

- [x] 3.1 修改 `src/ai-panel.ts`，让渲染流程消费 view model，并删除其中重复的状态到显示内容与操作可用性判断
- [x] 3.2 保持 `setupAiPanel()`、`AiPanelActions`、事件绑定、输入读取与清理、焦点、面板开合和滚动协调的现有接口及行为
- [x] 3.3 运行并按需要补充 `tests/ai-panel-dom.test.ts`，确认既有节点显示与所有 action 接线未改变

## 4. 回归验证

- [x] 4.1 运行 AI 面板显示决策、DOM、状态、临时对话和滚动聚焦测试，确认全部通过
- [x] 4.2 运行完整前端测试、`npm run typecheck` 和 `npm run build`，确认重构未引入回归
- [x] 4.3 通过面板显示决策与 DOM 回归测试检查首次召唤、思维扩展、配置缺失、成功、失败重试、追问及编辑重发流程，确认 UI、文案、操作与滚动行为保持不变，且 AI 面板不存在写入草稿本或正文本的入口
