## 1. 共享模型与兼容边界

- [x] 1.1 盘点 `nodeSize`、`positionSize`、`collectLines`、`collectBlocks` 和 `linkHrefAt` 当前读取的字段与边界行为，锁定迁移前基线
- [x] 1.2 创建纯函数共享文档模型模块，提供唯一节点位置尺寸计算和共享块遍历记录
- [x] 1.3 为文本节点、普通节点、空块、嵌套列表和边界位置补充共享模型单元测试

## 2. 调用方迁移

- [x] 2.1 迁移 `structured-notebook.ts` 的选区序列化相关遍历，保持现有输出和导出接口
- [x] 2.2 迁移 `format-commands.ts` 的格式状态分析，保持 `BlockInfo` 和 `FormatState` 结构
- [x] 2.3 迁移 `editor.ts` 的链接位置查找和节点位置计算，删除等价的本地实现
- [x] 2.4 检查调用方，确保不再存在重复的节点位置算法或块递归位置计算

## 3. 回归验证与交付

- [x] 3.1 运行结构化文档、选区序列化、格式命令、链接和富文本编辑器定向测试
- [x] 3.2 运行类型检查、lint、构建和完整项目检查，确认持久化 JSON 与 AI 选区行为不变
- [x] 3.3 更新路线图第二批任务证据，验证 OpenSpec change 并归档
