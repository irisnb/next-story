# 任务：恢复选区 AI 及时召唤入口

## 1. 后端：提示词结构与召唤协议

- [x] 1.1 在 `src-tauri/src/llm_config/generate.rs` 中提取红线层提示词常量并建立入口/语境组装函数
- [x] 1.2 `AiMessageKind` 增加 `SummonFirst` 并完成召唤首轮校验与组装
- [x] 1.3 `ai_replay_history_in_dir` 增加会话来源参数并完成前端绑定
- [x] 1.4 完成召唤组装、空选区拒绝、重放来源和红线完整性测试

## 2. 前端传输层：SummonFirst 接入

- [x] 2.1 `GenerateAiRequest` 增加召唤变体并将召唤路由到常驻会话，删除 legacy 降级分支
- [x] 2.2 传输层记录对话发起方式并在恢复重放时传递来源
- [x] 2.3 完成召唤流式、legacy 移除和重放来源测试

## 3. 前端流程：召唤首轮与替换语义

- [x] 3.1 提取共享首轮流程并接入直接提问与召唤
- [x] 3.2 实现召唤动作及已有对话/流式中的安全替换
- [x] 3.3 恢复 `onRetry` 真实重试语义并接入召唤流程
- [x] 3.4 完成召唤首轮、门禁、替换、恢复和追问测试

## 4. 浮动入口装配与思维扩展清理

- [x] 4.1 `selection-entry.ts` 改为单动作按钮并保留定位与防重复逻辑
- [x] 4.2 重新装配 selection-entry
- [x] 4.3 删除思维扩展 DOM、状态机死代码和相关测试
- [x] 4.4 删除旧首轮请求 shim 及其测试依赖
- [x] 4.5 完成自动化全量测试；真机验收仍需在 Tauri 应用中执行

## 5. 文档与记录修正

- [x] 5.1 修正 `AGENTS.md` 当前实现摘要
- [x] 5.2 更新 `README.md` 已实现能力清单与选区入口表述
- [x] 5.3 核对决策记录与实现，保留历史叙述并以第八节结论为当前决定
