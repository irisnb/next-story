## 1. 结构化生成契约与 Rust 无状态请求

- [x] 1.1 扩展前后端共享类型与桥接测试，先用失败测试锁定首次召唤载荷、追问完整轮次载荷和禁止前端 system 消息
- [x] 1.2 在 Rust 生成测试中加入合法追问、空白内容、非法角色顺序、待回答问题后仍有消息、错误脱敏和不隐式添加或压缩上下文场景
- [x] 1.3 实现结构化生成请求校验与无状态 OpenAI-compatible 消息组装，同时保持首次请求、唯一 LLM 配置和非流式响应行为
- [x] 1.4 运行桥接与 Rust 定向测试，确认请求过长和无效载荷返回稳定中文安全错误且不泄露对话全文

## 2. 前端线性临时对话状态

- [x] 2.1 为 AI 面板状态新增失败测试，覆盖冻结选区锚点、首次成功后开放追问、线性追加、一个待回答轮次和新召唤替换旧对话
- [x] 2.2 实现“一个锚点 + 成功轮次 + 可选待回答轮次”的内存状态，并保持面板打开状态与对话状态分离
- [x] 2.3 新增失败测试并实现追问失败保留、原样重试、只修改当前失败问题后重发，以及首次失败仍按原快照重试
- [x] 2.4 扩展滚动状态测试与实现，使同一对话追加轮次不错误重置，替换为新冻结选区时按新对话重置

## 3. 单请求协调与结果隔离

- [x] 3.1 扩展请求协调测试，覆盖首次召唤、追问和两类重试共用同一 busy 锁且不建立队列
- [x] 3.2 扩展 AI feature 路由测试，覆盖追问完整载荷、失败轮次路由、配置缺失返回和配置页往返不自动请求
- [x] 3.3 实现追问请求编排，并以作品 token、请求 token、临时对话 identity 和待回答轮次 identity 拒绝迟到结果
- [x] 3.4 增加同一对话中失效轮次、新召唤替换旧对话、作品卸载或替换后旧追问结果被忽略的回归测试

## 4. AI 面板追问交互

- [x] 4.1 新增面板 DOM 测试，确认召唤前和首次生成期间没有可提交输入，首次成功后才出现可用追问输入
- [x] 4.2 实现线性纯文本轮次渲染、非空白追问提交和生成期间禁用发送，并保持 Markdown 或 HTML 只按文字显示
- [x] 4.3 实现逐轮错误、原样重试与失败问题编辑重发交互，保证此前成功轮次不被替换或复制
- [x] 4.4 新增并通过零写回测试，确认追问输入、提交、成功、失败和重试都不会改变草稿本或正文本，也不存在应用入口

## 5. 生命周期与当前产品行为

- [x] 5.1 新增生命周期失败测试，覆盖面板收起再展开和 LLM 配置页往返保留当前临时对话且不自动请求
- [x] 5.2 实现返回欢迎页、卸载或替换作品时清空临时对话，并确认应用重启不恢复任何轮次、草稿或错误
- [x] 5.3 保持选择入口测试通过：首次召唤仍要求有效选区、召唤时无文字输入，编辑器变化不偷换冻结锚点
- [x] 5.4 手动运行真实模型冒烟流程：无输入召唤、两轮追问、失败后重试、新选区替换旧对话、配置页往返和作品切换隔离

## 6. 文档与完整验证

- [x] 6.1 更新 README 的当前状态、已实现能力、当前未实现和 AI 数据流，明确追问地基已实现但历史、持久化、思维扩展、思考收束和额外上下文仍未实现
- [x] 6.2 运行 TypeScript 类型检查、全部前端测试、前端构建和全部 Rust 测试，修复仅由本 change 引入的问题
- [x] 6.3 对所有改动文件运行诊断与差异检查，确认无类型抑制、无秘密泄露、无两个本子写回路径且只修改本 change 范围
- [x] 6.4 对照 proposal、design 与全部 delta specs 完成逐项人工验收，并运行 `openspec validate add-summon-ai-follow-up --strict`
