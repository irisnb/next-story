## 1. 设计令牌定义

- [x] 1.1 在 `:root` 定义颜色令牌：主色钴蓝、辅助暖金、中性墨黑/白/表面色、语义成功/危险/提醒
- [x] 1.2 在 `:root` 定义边框令牌：结构线宽度与颜色，含 chrome（顶栏/标签/工具栏/AI 面板）与文本编辑区两档
- [x] 1.3 在 `:root` 定义阴影令牌：硬边偏移阴影，含 chrome 与文本编辑区两档（文本区最重）
- [x] 1.4 在 `:root` 定义圆角令牌：无或极小值

## 2. 组件样式迁移到令牌（新粗野主义换肤）

- [x] 2.1 迁移顶栏（`.editor-header`）与按钮组样式，引用令牌
- [x] 2.2 迁移标签页（`.editor-tabs` / `.tab-btn`）样式，激活态用主色钴蓝
- [x] 2.3 迁移左侧竖排工具栏（`.format-toolbar`）样式，引用令牌
- [x] 2.4 迁移正文编辑区（`.notebook-textarea`）样式，使文本区框成为全窗口最重焦点
- [x] 2.5 迁移 AI 面板（`.ai-panel`）样式，引用令牌
- [x] 2.6 迁移格式抽屉（`.format-drawer`）样式，引用令牌
- [x] 2.7 迁移欢迎页/新建作品/LLM 配置/离开对话框/右键菜单/链接弹窗等表单与弹层样式，引用令牌

## 3. 选中态与主色语义

- [x] 3.1 统一选中/激活态（`.tab-btn.active`、`[aria-pressed="true"]`、`[aria-expanded="true"]`）为主色钴蓝，不用黑色
- [x] 3.2 主操作按钮（保存/创建/保存配置等）用主色平涂

## 4. 验证与收尾

- [x] 4.1 运行类型检查、前端测试、构建、Rust 测试
- [x] 4.2 更新因换肤失效的样式相关测试（改为断言令牌或 class 状态，不写死颜色值）
- [ ] 4.3 Windows 真机视觉验收：窄窗口阴影裁切、中文输入、长文阅读可读性、选中态主色观感
- [x] 4.4 确认 DOM 结构与行为逻辑无变化（现有 spec 行为测试仍通过）
