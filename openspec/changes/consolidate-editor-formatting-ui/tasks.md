## 1. 左侧竖排工具栏

- [ ] 1.1 `index.html` 把顶部横排 `#format-toolbar` 改为左侧竖排结构，保留高频项：段落样式、粗体、斜体、下划线、删除线、无序/有序列表、撤销、重做、查找入口、格式抽屉开关
- [ ] 1.2 `src/styles.css` 竖排工具栏布局（左侧固定宽度、纵向排列、与现有设计语言一致）
- [ ] 1.3 移除「更多」菜单（高频项已直接可见，低频项进抽屉）；清除格式命令移入抽屉
- [ ] 1.4 `src/dom.ts` 与 `src/editor.ts` 的工具栏 wiring 更新（`renderToolbar` 状态、命令监听、`mousedown preventDefault` 列表）

## 2. 左侧格式抽屉

- [ ] 2.1 把 `#format-drawer` 从右侧移到左侧，与 AI 面板（右侧）不再争抢空间
- [ ] 2.2 抽屉内按「字符格式 / 段落格式」两节重组，各节可折叠（disclosure），折叠状态会话内保持
- [ ] 2.3 确认全部字符与段落格式控件都在抽屉内可找到（全量分类目录）

## 3. 自动隐藏

- [ ] 3.1 抽屉：鼠标移出抽屉区域即缓缓收起（`mouseleave` + 过渡动画）
- [ ] 3.2 左侧竖排工具栏：不使用时缓缓隐去，鼠标移到窗口左边缘时重新唤起
- [ ] 3.3 确认自动隐藏不改变任何命令状态、选区规则与快捷键行为

## 4. 查找栏可见入口

- [ ] 4.1 左侧工具栏加「查找」按钮，点击与 `Ctrl+F` 等效（打开查找栏并聚焦查找框）

## 5. 右键菜单精简

- [ ] 5.1 移除右键菜单里的下划线、删除线、清除字符格式、清除段落格式；保留剪切/复制/粘贴/粘贴为纯文本 + 链接命令（打开/编辑/移除/创建）

## 6. 编辑器留白调节（显示偏好）

- [ ] 6.1 左侧工具栏加「留白」调节入口（预设档位或滑杆），作用于 `.notebook-textarea` 的内容区 padding
- [ ] 6.2 留白作为显示偏好持久化到 `localStorage`，重开恢复，缺失回退默认档位
- [ ] 6.3 确认留白不写入草稿本/正文本 JSON，也不进入 AI 选区快照

## 7. 验收

- [ ] 7.1 `npm run check` 全量通过（typecheck / lint / test:frontend / build / test:rust）
- [ ] 7.2 Windows 桌面真机验收：左侧工具栏/抽屉/自动隐藏/查找入口/右键精简/留白，用户确认后才归档
