# AGENTS.md：Next Story 协作宪法

> 任何 AI 助手进入项目，先读本文件，再做事。
> 本文件只写项目级红线和协作规则；长期使命见 `方向/核心方向宪章.md`，第一版取舍见 `方向/第一版方向共识-2026-07-01.md`。

## 产品是什么

Next Story 是**辅助剧本创作的陪想软件**。

用户在**写作空间**里亲手创作。**常驻 AI 面板**像一个随时可对话的聊天窗口，与写作区并列存在、可收起。用户直接提问，AI 陪想。

产品使命是降低开始创作和获得帮助的前置门槛，把认真讲述自己故事的权利交给每一个想创作的人。工具降低的是起步与求助门槛，不替代创作所需的感受、思考、判断、实践与坚持，也不承诺转移专业创作能力或保证优秀结果。

核心形态一句话：

> 用户在写作空间里亲手写；常驻 AI 面板像聊天窗口一样随时对话，由陪想 Agent 自动理解作品、准备上下文并回应。

## 陪想 Agent 的方向（长期，未完全实现）

AI 面板背后是**陪想 Agent**：

- 用户**直接提问**，不需要先配置上下文，也不需要先选中文字。
- Agent 自动理解作品、准备上下文并回应问题。
- 内容树（文件夹 + 文档）保留，但作为帮助 Agent 理解作品的**后台基础设施**；用户不必掌握内容树，也不必配置任何 AI 参考优先级。
- 旧「四级 AI 参考优先级」及继承、覆盖模型已**撤出用户概念**；未来 Agent 如何选择上下文是未定的内部方案，不得写成已确认方向。
- 选区保留为**可选的重点提示**（"请重点看这里"），不是 AI 使用前提；无选区也可直接提问。
- Agent 未来可以利用内容树、当前文档、当前写作位置、必要的已保存作品内容和允许参与本次思考的当前未保存内容。
- 后台预加载、检索、摘要、记忆和 Agent 循环都是**未来能力**，当前未实现，须逐个 OpenSpec change 定义，不假装已实现。
- 上下文说明采用**轻量可查看**：默认不打断对话，必要时用户可以查看大致参考了什么。
- AI 只提供观察、问题、假设和可能性：不替用户判断，不把假设冒充事实，永远不直接改用户文档。

## 绝对边界（违反即停下）

1. **AI 永远不直接改用户文档。** 不插入、不追加、不替换、不改写、不删除、不移动、不拆分、不合并、不就地分类，也不以其他方式整理其中字符；不做"一键应用到文档"。即使用户明确授权或确认，任何版本都不例外。AI 只能在用户文档之外给临时候选；用户自己复制、粘贴、编辑、保存。版本控制不算违反：恢复旧版本是用户主动重放自己写过的稿子，不是 AI 写入；但恢复前要提示影响范围。

2. **AI 输出永远是临时材料。** AI 面板里的内容不是作品事实。用户亲手复制、粘贴、编辑、保存后，才算进入作品文本。就算用户说"帮我直接写进去"，也要提醒这条边界。

3. **判断权永远在用户手里。**
   - **内容权**：什么进入作品，什么留下、删除或修改，由用户决定。
   - **解释权**：AI 可以提出有依据的假设，但不能把假设冒充作品事实或垄断解释。
   - **评价权**：AI 不替用户判断创意高低，不用单一理论判定故事正确、错误或是否优秀。
   - **方向权**：AI 可以打开不同可能，不能替用户决定故事往哪里走。

4. **一次只开一个 change，做完归档，再开下一个。** 禁止多个变更并行。旧项目死于同时开七八个 change、边界互相覆盖。

5. **一个概念只能有一个名字。** 出现第二个名字，先停下统一命名，再继续。旧项目死于 `main` / `screenplay` 双名分裂。已定：承载文件夹和文档的容器，中文统一叫「作品」，不叫「项目」；`project` 是它的代码英文名，与「作品」一一对应。（「旧项目死于」里的『项目』指上一个小软件项目，不是作品容器。）

6. **UI 里能选的每个模型，后端必须真实可调用。** 列表 = 实际支持。禁止展示后端跑不通的模型选项。

7. **代码写出来之前，用户必须先确认"要做什么"。** 先 OpenSpec propose，用户确认后再 apply。不因为"看起来简单"跳过设计。方向文档不是实现计划，不自动成为 change 范围。

8. **不得把降低门槛写成能力或结果承诺。** 不宣称工具能把专业创作能力转移给用户；不保证用户无需学习、思考或实践就能写出好故事。

9. **判断一个功能进不进版本的标准**：它是否帮助用户重新看见故事、是否保护用户判断权，而不是它看起来是否聪明。

## 架构责任边界（当前实现）

- **Rust / Tauri 后端是作品数据的可靠保管者**：内容树、文档保存、格式校验、版本迁移、事务一致性和崩溃恢复等确定性工作。
- **DSH 是作品内容的智能使用者**：AI 推理、思考回应和陪想能力。AI 需要参考作品时，只能通过受控只读接口取得被允许提供的内容；AI 运行环境不获得写入用户文档的能力。
- **作品数据与 AI 路径必须隔离**：作品服务不能注册为 AI 可调用的写文件工具。任何 AI 输出都只能留在用户文档之外，用户亲手复制、编辑并保存后才成为作品内容。

## 怎么协作（OpenSpec 流程）

```text
propose（写清要做什么）→ 用户确认 → apply（实现）→ archive（归档进真相源）
```

代码写出来之前，用户一定先看过并同意"要做什么"。一次只开一个 change。

## 当前实现的诚实边界

当前已实现（2026-08 常驻会话改造后）：

- **常驻 AI 会话**：直接提问是唯一首轮入口（选区作为可选重点提示自动附带）；回复**流式逐字呈现**；追问只发送增量问题，历史由常驻会话在驱动进程内维护。
- 首次回应成功后的**线性临时追问**；可取消生成。
- **上下文自动压缩**：长对话由框架压缩承接，超限报错提示新建对话，绝不静默砍历史。
- **崩溃恢复**：驱动进程意外退出后自动重启，重放显示历史恢复会话；临时对话不跨应用重启持久化，不提供历史会话列表。
- **旧选区工具已退场**：`AI 及时召唤`、`思维扩展`、选区浮动入口已从产品移除（代码保留未删）。

完整行为以 `openspec/specs/`（尤其 `resident-ai-session`、`persistent-ai-panel-entry`、`ai-thinking-panel`、`ai-feature-orchestration`）为准。当前没有：Agent 自动上下文（让 AI 看见作品是下一步，见 `方向/行动计划-2026-08-27.md` 事项 2）、后台预加载、检索、摘要、记忆、Agent 循环、多对话或历史列表、持久化会话、AI 内容库或用户确认的作品信息——这些都不是当前能力，不能写成已实现。

## 文档地图

| 位置 | 是什么 | 更新频率 |
|------|--------|----------|
| `AGENTS.md` | 项目宪法，本文件，你我都先读它 | 铁律迭代时 |
| `反面案例/` | 旧项目教训，只作反例，定稿别动 | 几乎不变 |
| `方向/核心方向宪章.md` | 长期使命和永久产品立场的完整真相源 | 方向级决策时 |
| `方向/第一版方向共识-2026-07-01.md` | 第一版范围、当前/未来区分、统一词汇 | 方向调整时 |
| `.omo/二阶段-01-编辑器、项目结构与UI方案/` | 二阶段阶段性讨论记录（编辑器三轮、作品结构等）；其中「AI 参考优先级」相关结论已被 2026-08 方向更新取代，不再作为用户概念 | 持续修订 |
| `openspec/specs/` | 真相源：我们"已经做成"什么（实现并归档后才有） | 工具自动驱动 |
| `openspec/changes/` | 正在进行的变更 | 工具自动驱动 |

## 给 AI 的话

### 关于用户

用户是开发小白，有一点经验，但极少，而且上一个项目失败了。不要假设用户懂任何技术术语，出现术语当场用大白话解释；每一步操作讲明白为什么做、在干什么、可能出什么问题；报错、英文、命令行输出主动翻译成中文。用户看不懂就是没讲清楚，不是用户的问题。

### 关于沟通与创作判断

- 不要高情商发言、不要彩虹屁、不要"这个问题问得好"；客观、理性、直接。用户判断错了，直接指出来并给理由，不要附和。先陈述事实，再给建议。
- 用户是资深动画/影像创作者，技术概念能用影视类比讲清就用；简单的事直接说清楚，别绕远。
- 不把故事或创意分成高级/低级、常规/非常规、正确/错误；临时分类只用来帮助理解，不当规矩。不把怪想法改正常，也不为"更大胆"强行推怪。先看用户在意什么，再递有依据的问题和可能性。
- 不把任何剧作理论当成所有故事必须遵守的普遍规律。
- AI 只提供观察、问题、假设和可能性，不替用户判断，不把假设当作品事实。

### 关于文本区的绝对边界

- 内容树中的文档只由用户亲手产生内容。
- AI 可以建议、提问、给候选，但不能以任何理由向这些文档写入、插入、追加、删除、移动或替换任何字符。
- 用户要用 AI 输出，也必须自己复制、粘贴、编辑。
- 这是产品灵魂，不是技术限制。

### 关于做事

- 不确定就问，不要猜。
- 不要因为"看起来简单"就跳过思考。
- 不要在用户没确认时扩大范围、多做功能。
- 旧项目文档只是反面证据，不是新项目规范。
- 每次 git 提交、推送、合并或改写历史，都必须先加载 git-master skill 并按其规范执行：先查状态、精确暂存、按项目风格写中文提交信息、不 force-push。
