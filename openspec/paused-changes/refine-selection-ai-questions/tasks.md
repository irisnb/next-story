## 1. 锁定固定思考任务契约

- [ ] 1.1 在 `src-tauri/tests/llm_config_test.rs` 更新请求捕获测试，断言 system message 包含局部提问职责、可变问题数量、非评判和不代写边界，同时 user message 仍只含原冻结选区
- [ ] 1.2 在 `src-tauri/tests/llm_config_test.rs` 保留并补强无附近文本、摘要、作品元数据、角色世界观、会话历史或知识库内容的请求断言
- [ ] 1.3 在 `src-tauri/tests/llm_config_test.rs` 断言一次召唤仍只产生一次非流式 `chat/completions` 请求，不增加分类或检索调用

## 2. 实现第一阶段局部提问策略

- [ ] 2.1 在 `src-tauri/src/llm_config/generate.rs` 重写后端固定方法提示词，加入抽象材料打开可能性、具体材料贴文观察和模糊材料自然混合的阅读指导
- [ ] 2.2 在固定方法提示词中加入可选麦基式观察入口、明确观察与开放问题边界，以及问题数量不固定的要求
- [ ] 2.3 在固定方法提示词中加入简短总览、分点展开、简短收束的可读层次，并保持纯文本、不代写、不评分、不判断作品优劣

## 3. 验证行为与边界

- [ ] 3.1 运行 Rust LLM 配置测试，确认请求结构、错误处理、大小限制和秘密信息保护仍全部通过
- [ ] 3.2 运行 `npm run check`，确认 TypeScript、前端测试、生产构建和 Rust 测试全部通过
- [ ] 3.3 使用抽象词“背叛”、极短具体表达和较完整叙事片段各执行一次真实模型人工检查，记录是否贴合材料、机械凑数、武断推断、输出理论课或代写，不把样例输出固化为精确字符串测试
