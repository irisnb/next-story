# Tasks: 独立对话与可靠保存（阶段 2）

## 1. 讨论身份与消息隔离

- [x] 1.1 定义并实现全局唯一 `conversation_id` 生成（时间戳 + 随机段），落在前端共享模块
- [x] 1.2 `ai-session-transport` 消息编号改为 `{conversation_id}:msg-{n}`，支持多讨论的流式目标路由
- [x] 1.3 Rust `dsh_driver` 终态索引兼容带前缀消息身份（键形状不变），补测试证明跨讨论消息唯一
- [x] 1.4 迟到结果按讨论身份隔离：已删除/已离开讨论的 delta 与终态一律丢弃

## 2. 讨论档案存储（Rust）

- [x] 2.1 新增 `conversation_store` 模块：档案结构（version 1、conversation_id、时间、关注文档身份、turns、首轮材料来源），路径 `<作品>/next-story-system/conversations/<id>.json`
- [x] 2.2 实现 list / save / delete 前端命令，有界读取与 JSON 校验
- [x] 2.3 保存使用原子写入（复用 `write_file_atomically`），损坏/超限档案跳过并返回可见提示
- [x] 2.4 单元测试：保存、读取、删除、损坏跳过、超限跳过、目录扫描列表
- [x] 2.5 确认讨论档案命令不注册为 AI 可调用工具、不进入 `capability_gateway`

## 3. 前端状态模型改造

- [x] 3.1 `ai-panel-state` 改为「当前讨论引用 + 讨论集合轻量视图」，保持 facade 与订阅语义
- [x] 3.2 新召唤、直接提问首轮、「新建对话」开启新讨论并保留旧讨论（reducer 迁移）
- [x] 3.3 每讨论单请求锁：同一讨论内拒绝并发请求，不同讨论互不阻塞
- [x] 3.4 切换作品清空当前视图并加载新作品讨论列表；切换文档不清空讨论、不改绑关注文档

## 4. 保存与恢复

- [x] 4.1 用户轮接受即保存（终态 pending），生成终态（成功/失败/取消）原子更新
- [x] 4.2 保存失败对用户可见，不伪装成功
- [x] 4.3 应用重启后按当前作品加载讨论列表
- [x] 4.4 重开讨论显示已保存轮次；未完成轮显示中断，不自动重发、不重复生成
- [x] 4.5 DSH 会话与讨论同寿命：重开发新追问时经 replay 重建会话，删除讨论与应用退出时结束会话

## 5. 会话列表与讨论 UI

- [x] 5.1 会话列表入口与条目（标题取首轮问题/召唤文本截断，空则时间；显示终态与时间）
- [x] 5.2 删除讨论为独立动作（明确入口与确认），不因关闭/切换触发
- [x] 5.3 面板显示当前讨论，支持从列表切换讨论；空状态可直接提问

## 6. 规格与文档同步

- [x] 6.1 按 project-mission-governance delta 更新 AGENTS.md 当前实现摘要
- [x] 6.2 按 project-readme delta 更新 README 已实现/未实现表述

## 7. 验证

- [x] 7.1 `npm run check` 通过
- [x] 7.2 `npm run test:rust` 通过
- [x] 7.3 现有 AI 相关测试全绿（含崩溃恢复、召唤、追问语义回归）
- [x] 7.4 手动走查：重启恢复列表、切换作品隔离、删除讨论、迟到结果丢弃、保存失败提示、中断轮不重发
