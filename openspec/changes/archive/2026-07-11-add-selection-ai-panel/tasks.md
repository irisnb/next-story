## 1. 锁定选区快照与浮动入口

- [x] 1.1 为 `SelectionSnapshot` 有效性、原文保留、草稿本/正文本来源和点击后冻结行为编写失败测试
- [x] 1.2 实现与 `textarea` 隔离的选区适配器，只向外暴露不可变快照和可见锚点，不让后续 AI 模块读取编辑器 DOM
- [x] 1.3 为选区状态控制器编写失败测试，覆盖空白选区、点击别处、切换本子、焦点端可见性、滚动重定位和滚出视区隐藏；不使用 Node 假布局证明像素几何
- [x] 1.4 实现以 `selectionEnd` 焦点端为锚点的“召唤 AI”浮动入口，并在真实浏览器/Tauri 中验收鼠标选择、键盘选择、滚动、换行、字体和 DPI 下的实际定位

## 2. 建立可扩展但最小的 AI 前端核心

- [x] 2.1 为 `single-in-flight` 请求协调器编写失败测试，证明重复召唤不会执行第二个 client 调用、编辑器继续可用且请求结束后解锁
- [x] 2.2 为正交面板状态编写失败测试，覆盖 visibility 与 request 独立变化、收起期间完成不自动展开和 `replace-current` 结果策略
- [x] 2.3 实现单请求协调器和面板状态模型，覆盖 idle/loading/success/error/configuration-required，并以作品实例令牌隔离迟到结果
- [x] 2.4 为失败重试编写测试，证明重试使用原冻结快照、不自动重试且不读取当前新选区
- [x] 2.5 为作品卸载、作品替换和进入配置页期间的迟到请求编写测试，证明旧结果不污染新作品且配置页不触发第二个调用

## 3. 实现唯一 LLM 配置驱动的真实生成

- [x] 3.1 为 Rust 生成用例编写失败测试，覆盖后端固定 Prompt 组装、保存配置加载、配置缺失、合法非空 assistant 回复和 2xx 无效回复
- [x] 3.2 提取连接测试与生成共用的安全 HTTP 客户端、`chat/completions` URL 和响应解析基础，不改变现有 URL/HTTPS/重定向规则
- [x] 3.3 实现 `generate_ai_thinking` Rust 用例和 Tauri command，只接收选区原文，由后端加载唯一保存配置并集中组装固定首版 Prompt
- [x] 3.4 定义 `GenerateAiError { code, message }` 并为认证、网络、超时、请求过长、服务错误、空回复及配置损坏增加测试，确保错误不包含 API Key、Authorization、请求正文或完整远端响应
- [x] 3.5 在前端桥接层增加窄类型生成 API，并增加测试证明生成链路没有草稿本、正文本或保存命令写入参数

## 4. 构建独立 AI 面板与配置引导

- [x] 4.1 增加右侧约 420px AI 面板、收起/展开控件和响应式 DOM/CSS，桌面并排、窄窗口覆盖，并保留编辑器为视觉主区
- [x] 4.2 将浮动入口、请求协调器、生成桥接和面板状态接入编辑器，确保收起不清空、不因完成自动展开、新请求替换当前结果、重启不恢复
- [x] 4.3 使用纯文本绑定显示选区、回复和错误，保留换行与标准复制能力，并验证 Markdown/HTML 标记不会被解析或执行
- [x] 4.4 接入缺少配置状态和“前往配置”入口，验证返回编辑器后保留作品、未保存文字和原快照，但不自动发请求
- [x] 4.5 验证 loading 期间编辑、切换本子和新选区仍可用，但第二次召唤不可用；成功或失败后新的有效选区可再次召唤

## 5. 锁定零写回边界与端到端验证

- [x] 5.1 增加模块依赖与行为回归测试，证明 AI 模块不持有编辑器写入、`saveProject` 或等价回调，成功、失败、重试均不改变两个本子
- [x] 5.2 运行 `npm run typecheck`、`npm run test:frontend` 和 `npm run test:rust`，确认全部新增及既有测试通过
- [x] 5.3 运行 `npm run build`、`openspec validate add-selection-ai-panel --strict` 和 `openspec validate --all --strict`，确认构建与规格无回归
- [x] 5.4 在真实 Tauri release 中使用已保存的唯一 LLM 配置验证：鼠标/键盘选区、浮动入口、真实回复、面板收起恢复、替换当前结果、错误重试和缺配置引导
- [x] 5.5 人工核对草稿本与正文本文件在 AI 成功、失败和重试前后完全不变，并确认界面不存在插入、替换或应用入口
- [x] 5.6 实现和验收完成后更新 README，只陈述“只使用选区原文的 AI 最小实验闭环”，不得宣称带上下文的完整第一版 AI 闭环，并继续列明未实现的上下文、多轮、历史、流式和 AI 内容库
