## 1. 结构化文档契约

- [x] 1.1 先为格式版本 1 的精确外层字段、无块最小文档与空段落区分、首尾与连续空段落、节点字段、属性、mark 顺序与去重、相同 marks 相邻文本必须合并、文本禁含 CR/LF、文本禁含孤立代理项（U+D800–U+DFFF）、列表起始编号 `1 ≤ start ≤ 2^53-1` 且实际编号不溢出、禁止嵌套列表和未知结构补充前端失败测试，再实现共享 TypeScript 文档类型、规范输出和严格校验函数。
- [x] 1.2 用同一组规范与非规范 JSON 样例先补充 Rust 失败测试，再实现后端结构化本子 DTO 和独立 grammar 校验，确保前后端对额外字段、空数组、marks、列表结构、孤立代理项和整数域范围的判定一致。
- [x] 1.3 将项目结构版本从 1 升级为 2，并把新建项目测试和实现切换为 `作品文本/草稿本.json`、`作品文本/正文本.json` 及包含一个空 `paragraph` 的有效空白文档；补充版本 1、未知未来版本、旧 `.txt` 项目、`project.json` 损坏、版本字段缺失、版本为字符串 `"2"`、版本为 `null`、版本为小数或负数以及不支持版本伴随中断事务时全部文件字节不变的失败测试，不增加迁移或兼容读取。

## 2. 项目打开与事务保存

- [ ] 2.1 先补充损坏 JSON、不支持文档版本、未知节点/标记、非法嵌套、超限 `.json` 和只含旧 `.txt` 项目的打开失败测试，再实现打开前校验和中文错误映射，证明失败不会生成空白替代或修改原文件。
- [ ] 2.2 先将成功保存、暂存失败、提交中断和恢复测试改为两份 `.json` 与 `project.json` 的同世代事务，再切换路径、暂存文件和恢复实现，并在恢复内容进入编辑器前重新校验。
- [ ] 2.3 补充后端收到非法保存载荷时在创建暂存文件前失败的测试，并确保现有可见文件和已提交世代保持不变。

## 3. Tiptap 基础富文本内核

- [ ] 3.1 安装并只注册标题、粗体、斜体、无序列表、有序列表和列表项所需的 Tiptap 2.27.2 扩展，补充 schema 不接受链接、图片、表格和三级标题的测试。
- [ ] 3.2 先把纯文本适配测试改为结构化文档的空白、首尾与连续空段落、中文/emoji、相邻同 marks 文本规范形态、标题、粗斜体和两种列表组合往返测试，再将编辑器适配层改为完整 Tiptap JSON 导入、导出和销毁。
- [ ] 3.3 将编辑器控制器、项目打开 DTO 和保存快照改为 `draft` / `main` 两份结构化文档，并补充初始化或切换本子不制造用户 transaction、不污染另一历史的测试。
- [ ] 3.4 先补充仅格式变化、恢复保存基线、保存期间晚到编辑和失败保存的状态测试，再按规范结构值实现未保存判断和冻结文档保存基线。
- [ ] 3.5 补充打开其他作品、返回欢迎页和关闭窗口时只有格式变化的离开保护测试，确保结构化保存状态是提示、保存并离开、取消及保存失败保留当前文档的共同事实源。

## 4. 工具栏命令与界面

- [ ] 4.1 先补充无选区禁用、段落样式统一/混合、粗斜体三态、标题降级成列表、两类列表互换、混合块统一、无序及有序列表部分拆分并保持未触及编号、同类列表恢复正文、清除格式和当前本子历史的命令测试，再实现不产生嵌套列表的选区分析和 Tiptap transaction。
- [ ] 4.2 在本子标签下方实现固定工具栏和可访问状态：正文/一级标题/二级标题、“多种格式”、半选态、禁用态及撤销重做可用性，并确保只操作当前本子。
- [ ] 4.3 实现 `Ctrl+B`、`Ctrl+I`、`Ctrl+Z`、`Ctrl+Y`、`Ctrl+Shift+Z`，补充只有光标时粗斜体快捷键不启动后续格式、两种重做快捷键等价的测试。
- [ ] 4.4 实现 A2 窄窗口布局，在“更多”菜单收纳清除格式、撤销和重做；补充 DOM/CSS 契约测试，并在桌面和窄窗口浏览器视口检查无溢出、遮挡或状态丢失。

## 5. 受控粘贴

- [ ] 5.1 先为明确 HTML 元素映射、`br` 在正文/标题/列表项中的块拆分、普通容器与嵌入结构的明确元素集合判定、未知普通容器透明降级、混合类型嵌套列表按最近类型扁平化、同一 `ol` 被打断后分段保留编号、`li` 嵌套列表后尾随文字追加、`ol[start]` 与忽略的 `reversed`/`li[value]`、纯文本上下文、标题/粗斜体/列表保留及字体/颜色/链接/下划线清除补充失败测试，再实现 DOM 解析、归一化和插入前 schema 校验。
- [ ] 5.2 先补充表格按行转段落且单元格用 Tab 分隔、单元格内多块扁平化为空格连接的测试，再实现表格降级并证明不会产生表格节点或嵌套段落。
- [ ] 5.3 先用 CRLF、NBSP、行尾空白、单个末尾 LF、四种列表前缀、空行、Tab 和段落边界样例锁定 HTML 与 `text/plain` 的唯一比较函数，再补充图文混合、纯图片、投影不一致、只有 HTML 的未知可见嵌入内容和 schema 非法时整次拒绝测试，实现原子粘贴与中文提示并确保失败不改变文档或历史。
- [ ] 5.4 补充本子内复制、剪切和跨本子粘贴测试，证明纯文本与受控 HTML 都匹配选区，支持格式可往返且剪切只删除实际选中内容。

## 6. AI 选区纯文本快照

- [ ] 6.1 先为正文/标题、部分段落、粗斜体、前后空格、中文/emoji、固定 `- ` 与 `数字. ` 前缀、单 LF 块边界、无前导/尾随 LF、空段落/空列表项、完整与部分列表项、不同 `start`、相邻列表及跨列表到正文的选区补充精确字符串测试，再实现结构化切片的唯一纯文本序列化规则。
- [ ] 6.2 将冻结快照从全文 UTF-16 字符串切片改为 `draft` / `main`、Tiptap 有序位置和冻结 `selectedText`，并补充正反向选择内容相同、位置不持久化的测试。
- [ ] 6.3 补充首次请求、思维扩展预备态和后续追问期间继续编辑/改格式/切换本子的测试，证明所有请求只复用冻结纯文本且不发送 JSON、格式、全文或新增上下文。
- [ ] 6.4 按永久禁写 requirement 复核并测试 AI 回复前后两份完整 JSON、撤销历史和保存状态完全相同，且不存在从 AI/后台流程到编辑器命令、格式 transaction、IPC 或回调的写入通路；覆盖插入、追加、替换、改写、删除、移动、拆分、合并、分类、结构整理以及用户授权场景。

## 7. 文档、集成与验收

- [ ] 7.1 修正 `.omo/二阶段-01-编辑器、项目结构与UI方案/文本编辑器三步走路线.md` 中已失效的第二轮旧 `.txt` 迁移要求，并记录三轮编辑器完成归档后另开 DOCX change。
- [ ] 7.2 运行 `npm run typecheck`、`npm run test:frontend`、`npm run build` 和 `npm run test:rust`，修复本 change 引入的全部失败且不覆盖工作区既有用户修改。
- [ ] 7.3 在 Windows Tauri 2 / WebView2 手工验证微软拼音组合输入、选区、格式命令、复制剪切粘贴、双本子历史、保存重开、损坏项目中文错误和窄窗口 A2 工具栏，并记录结果。
- [ ] 7.4 用至少 20 万汉字且包含本轮格式组合的本子测量连续输入延迟 p95 与载入进程增量内存，确认分别不高于 50ms 与 300MB，并验证保存重开无文字、格式或选区损坏。
