## 1. 编辑器状态同步

- [x] 1.1 调整 `src/editor.ts` 的 `applyTree`，在接受删除后的新树时先同步 `currentState.tree`，再执行剩余文档切换或空态处理。
- [x] 1.2 保持用户取消丢弃未保存修改时的旧树、当前文档和未保存内容不变。

## 2. 回归验证

- [x] 2.1 在 `tests/editor.test.ts` 中补充当前文档删除后切换到剩余文档时 `getTree()` 与新树一致的断言。
- [x] 2.2 补充当前文档删除后进入空态时 `getTree()` 与空树一致的断言，并运行相关测试与类型检查。
