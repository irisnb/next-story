## 1. 入口结构与样式

- [x] 1.1 将选区入口触发器从小圆点改为带`AI`文字的胶囊按钮，并保证点击区域不小于 32px 高度、44px 宽度
- [x] 1.2 调整入口打开态、悬停态和菜单视觉，使胶囊入口与现有编辑器界面保持一致
- [x] 1.3 确认入口菜单仍只显示`及时召唤`和`思维扩展`，且菜单内不增加输入框或说明卡片

## 2. 定位与生命周期

- [x] 2.1 更新选区入口定位逻辑，使入口优先靠近 `selectionEnd` 焦点端最后一个可见字符右侧
- [x] 2.2 在右侧空间不足、越出编辑器边界或遮挡选区时，把入口避让到选区最后一行下方靠右的位置
- [x] 2.3 将入口最终坐标约束在当前编辑器可见内容边界内，并在焦点端滚出可视区域时隐藏入口
- [x] 2.4 菜单打开期间锁定触发器位置，避免编辑器失焦、AI 面板开合、窗口尺寸变化或布局变化导致入口跳位
- [x] 2.5 用户选择`及时召唤`或`思维扩展`后立即隐藏本次活选区入口，并防止面板开合让旧入口自动重新出现

## 3. 回归测试

- [x] 3.1 更新 `tests/selection-entry.test.ts`，覆盖胶囊入口尺寸、右侧定位、下方避让、边界约束和菜单打开不跳位
- [x] 3.2 回归`及时召唤`立即请求、`思维扩展`进入预备态、空白选区不显示入口、一个字选区可用入口
- [x] 3.3 运行前端相关测试和项目检查，确认本 change 没有改变 AI 面板临时材料边界或两个本子的写入规则
