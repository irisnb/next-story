# 第一版方向共识（2026-07-01）

> 本文档记录新项目第一版方向共识。
> 它不是实现计划，也不是最终规格。技术选型和具体交互细节尚未展开。
> 它只回答：这一版为什么做、做什么、明确不做什么、核心概念叫什么、数据谁说了算、怎么验证。
>
> 旧项目材料位于 `反面案例/`，仅作教训与反例参考，不作本项目规范。

## 0. 一句话定位

第一版是一个**带 AI 思考能力的剧本写作编辑器**：用户在类似 Word 的编辑器里写草稿和正文，需要时选中任意片段召唤 AI；AI 基于选区、上下文和内置内容库发散想法、提出好问题、帮助故事生长，但永远不直接改动用户文本。

更短地说：

> 用户写作，AI 陪想；AI 只能递材料，不能抢笔。

## 1. 为什么做（Why）

### 1.1 真实痛点来源

方向来自一位真实创作者（动画创作者 / AI 影视从业者）的亲身剧本创作经历。核心痛点按重要性排序：

1. **想不出来**：这是最痛、最根本的问题。想法最重要，有了想法再分门别类地整理不难；真正困难的是想不出来、接不下去。
2. **写作现场需要自由**：创作没有固定流程。创作者可能先写三句怪想法，也可能先写正文，也可能写一堆暂时说不清的碎片。工具不能把写作流程规训成固定步骤。
3. **灵感易逝**：片段、画面、黄段子、奇怪设定、角色语气来得快，去得也快，需要被接住。
4. **经验门槛**：用户不能默认都受过剧作训练。有想法但缺方法论的人，同样要能被陪着把故事往前推。

### 1.2 灵魂原则

> **好问题 > 好答案。有了问题才有答案。**

AI 的核心价值是"问对问题"，不是"给对答案"。但这里的"问题"不是剧作课检查表，而是陪用户看见材料可能怎么生长。

更底层的创作观是：

> 创作没有高低贵贱的材料，也没有固定方向。任何材料都可能在具体语境里开花。

所以工具不能预设哪种花更好。黄段子可以低俗，也可以好笑，也可以感动；常规桥段可以无聊，也可以长出不常规的东西；荒诞设定可以只是胡闹，也可以承载非常真实的情绪。

工具要做的是：

- 陪用户观察：这颗种子现在哪里在发亮。
- 帮用户施肥：递出问题、可能性、参考和发散方向。
- 保持用户判断权：写什么、留什么、删什么、改什么，永远由用户亲手决定。

### 1.3 创作过程的真实机制（设计依据）

从用户真实创作讨论中提炼出的机制，是本工具交互的依据：

- 创作本质是**持续的自我提问**，但问题不是固定表格，而是从眼前材料自然长出来。
- 创作常常从一颗种子开始：一句台词、一个黄色笑话、一个怪设定、一个普通场景、一个说不出口的动作。
- 重要的不是材料属于什么类型，而是它在当前语境里可能怎么开花。
- 很多不常规是从常规里长出来的。工具不能一味鼓励"更怪"、"更黄"、"更反常规"，也不能把常规材料判成无聊。
- AI 最有用的时候，不是替用户写，而是在用户卡住时帮他看见：这里可以往哪些方向继续长。

## 2. 第一版做什么（Scope）

### 2.0 第一版内部优先级：先核心闭环，再排后增强

第一版不是一次全做完，而是分两层。旧项目死于"每个功能单看都合理，合起来主链路验证变慢、边界互相覆盖"，所以这里先切一刀：

**A. 核心闭环（先做，先验收）**

```text
建作品 / 打开作品
  ↓
在草稿本、正文本里写字
  ↓
保存 -> 重新打开后内容还在
  ↓
选中片段召唤 AI
  ↓
AI 回应只进 AI 面板，不碰用户文本
  ↓
单个大语言模型能真实跑通
```

这条链路稳定、顺手、不越界，第一版就算立住了。

**B. 排后增强（仍属第一版，但核心闭环稳定后才做）**

- 内置 AI 内容库（让 AI 问得更懂剧作）。
- 简单版本控制（保存快照、查看、恢复）。

排后不代表砍掉，而是不和核心闭环抢地基。核心没稳之前，不动这两块。

### 2.1 主体形态：Word-like 剧本编辑器

第一版主界面不是 AI 聊天窗口，而是一个尽量笨、尽量稳定的文本编辑器。

它先解决一件事：

```text
用户能安心写剧本，写到某段想不出来时，可以选中片段召唤 AI 陪自己继续想。
```

第一版不追求专业剧本排版，不追求多轨编辑，不追求复杂结构化，只追求这条主链路稳定、顺手、不越界。

### 2.2 两个本子：草稿本与正文本

编辑器里至少有两个文本区域：

1. **草稿本**
   - 用来写碎片想法、试写段落、临时笔记、怪点子、黄段子、角色念头、还没成形的画面。
   - 草稿本可以乱。乱不是问题，它是苗圃。
   - AI 可以读取草稿本作为上下文，可以围绕用户选区发散，但不能自动整理、改写、移动或删除草稿本内容。

2. **正文本**
   - 用来写真正进入作品的剧本正文。
   - 正文本是第一版最重要的内容事实源。
   - 只有用户亲手输入、复制、粘贴、编辑并保存到正文本里的内容，才算作品正文。
   - AI 不能自动插入、替换、改写、移动或删除正文本中的任何字符。

草稿本和正文本没有高低贵贱，只有用途不同：草稿本是想的地方，正文本是作品当前样子的地方。

### 2.3 选区召唤 AI

用户在草稿本或正文本里像复制文字一样选中一段内容，即可召唤 AI。

选区不是唯一上下文，而是焦点。AI 实际应参考：

- 用户选中的片段。
- 选区前后的附近文本。
- 当前本子的全文或摘要。
- 当前片段来自草稿本还是正文本。
- 内置内容库里的剧作知识、样本、反例、提问原则。

AI 的任务不是判断这段对不对，也不是替用户改好，而是围绕选区递出：

- 发散方向。
- 更具体的问题。
- 可能的推进方式。
- 不同气质的展开。
- 可能被忽略的生长点。
- 帮用户看见这段材料正在往哪里长。

### 2.4 AI 面板

AI 的回应永远显示在独立的 AI 面板里，不自动进入草稿本或正文本。

第一版 AI 面板只需要提供轻动作：

- 复制。
- 继续问。
- 换个方向。
- 清空 / 收起。

第一版不提供，也后续不应该提供：

- 插入正文。
- 替换选区。
- 一键应用。
- 自动改写。
- 自动整理草稿或正文。

用户如果想使用 AI 给出的内容，必须自己复制、粘贴、编辑。这不是低效，而是本工具的权力边界：创作和更改的权利永远在用户手上。

### 2.5 内置 AI 内容库（第一版知识库过渡方案）

AI 要"懂剧作才能问好问题"，但完整知识库（LightRAG / 个人素材库 / 复杂检索）太重、太早。第一版用轻量方案：

- 先沉淀一批**内置 AI 内容库**：剧作方法、短剧/短片经验、样本分析、反例教训、提问原则、"陪它生长"的交互原则。
- 这批内容是**喂给 AI 的内功**，不是让用户管理的一堆资料库。
- 用户只感受到：AI 更会问、更懂创作、更能围绕选区发散。
- 等第一版主链路稳定后，再考虑用户自建内容库、导入样本、个人素材收集、结构化检索等能力。

### 2.6 简单版本控制

版本控制第一版只解决安全感：用户可以大胆试，试坏了还能回来。

第一版可以只做：

- 保存快照。
- 查看历史版本。
- 恢复旧版本。
- 给版本写简短备注。

不做复杂分支、合并、多人协作、精细 diff 树。

**版本控制不违反"AI 不改用户文本"这条铁律。** 版本记录的是用户自己写过、保存过的文本状态；恢复旧版本，本质是用户主动重放自己以前的稿子，内容仍然出自用户之手，工具只是把它记录下来再放回去，不生成任何新内容。这与"AI 主动改文本"是两回事。

唯一的数据安全要求：恢复前要提示影响范围（会覆盖当前哪个本子的内容），避免用户误操作冲掉还没保存的内容。

### 2.7 说明性文档（未来方向，不进第一版主链路）

未来可以增加一些辅助说明文档，例如：

- 世界观。
- 角色卡司表。
- 故事主题。
- 人物关系。
- 时间线。
- 设定说明。
- 创作备忘。

这类文档和草稿本/正文本不同。它们是地图，不是作品本身。AI 可以更强地辅助整理，但必须遵守：

```text
AI 生成候选整理 -> 用户确认 -> 才进入说明性文档
```

AI 可以自动整理候选，但不能绕过用户确认直接写入正式说明文档。说明性文档也不能反过来统治正文：它们帮助用户理解作品，不替用户规定作品必须怎么写。

## 3. 明确不做（Non-Goals）

以下能力不是永远不做，但明确不进第一版，记在案、不丢：

1. 专业多轨剧本编辑器、复杂剧本排版、分场导航、自动格式门禁。
2. AI 自动插入、替换、改写、移动、删除草稿本或正文本内容。**这条是永久边界，不是第一版限制。**
3. AI 自动整理、自动提取、自动分类草稿本或正文本。
4. 自动判断用户当前创作阶段。
5. 五模块结构化、复杂世界观/角色卡自动维护。
6. 用户自建知识库、LightRAG、复杂检索、长期记忆。
7. 矛盾检测、心流自动识别、涟漪检查、多 Agent。
8. 双文档 `main` / `screenplay` 语义。
9. 多 LLM provider 分裂配置（前端列一堆、后端不支持是旧项目 P0 雷）。

未来可以增强的方向包括：更强版本控制、更强知识库、用户素材库、说明性文档候选整理。但增强方向永远不能变成 AI 直接改草稿本或正文本。

## 4. 核心概念命名（一个概念只能有一个名字）

| 概念 | 名称 |
|------|------|
| 一部作品的容器 | 作品 |
| 用户自由写碎片、想法、试稿的地方 | 草稿本 |
| 用户写作品正文的地方 | 正文本 |
| 正文本中真正构成当前作品的文字 | 正文 |
| 用户框选一段文本给 AI 看 | 选中 / 选区 |
| 用户用选区触发 AI | 召唤 AI |
| AI 生成但尚未被用户复制进文本的内容 | 临时材料 |
| AI 展示临时材料的区域 | AI 面板 |
| AI 提问与发散所依赖的内置知识 | AI 内容库 |
| 世界观、角色表、主题、时间线等辅助资料 | 说明性文档 |
| 模型接入配置 | LLM 配置 |

（"作品"是唯一产品概念名；技术代码里可以用 project 等英文标识符，但那是代码层，不作产品名。其他技术层命名待技术选型阶段再定，此处只锁产品概念，且禁止 `main` / `screenplay` / `document` 这类模糊备用名同时出现。）

## 5. 数据事实源（谁说了算）

- **正文本是作品内容事实源**：用户写进去、留下来、保存下来的文字，就是当前作品正文。
- **草稿本是用户思考文本**：它可以混乱、临时、矛盾，但同样只能由用户亲手改动。
- **AI 面板输出不是事实源**：它是临时材料、候选、发散，不自动进入任何文本区。
- **用户复制/粘贴/编辑是唯一采纳路径**：AI 给出的任何文字，只有被用户亲手放进草稿本或正文本后，才成为项目文本的一部分。
- **说明性文档未来可由 AI 生成候选整理**：但必须用户确认后才进入正式说明性文档。
- **版本记录保护用户文本**：版本控制记录的是用户保存过的文本状态，不是 AI 替用户改稿的痕迹。

## 6. 铁律（旧项目血的教训）

1. **AI 永远不直接改草稿本和正文本。** 不插入、不替换、不改写、不删除、不移动，任何版本都不例外。
2. **AI 输出永远是临时材料。** 用户亲手复制、粘贴、编辑、保存，才算进入作品。
3. **判断权永远在用户手里。** 写什么、留什么、删什么、采纳什么，都由用户决定。
4. **创作没有素材等级。** 工具不能预设什么更高级、什么更正确、什么更该写。
5. **第一版只服务写作时想不出来。** 不叠复杂结构化、自动检测、自动整理、专业排版。
6. **一个概念只能有一个名字。** 出现第二个名字，先停下统一命名，再继续。
7. **UI 里能选的每一个模型，后端必须真的能跑通。** 列表 = 实际支持。
8. **判断一个功能进不进版本的标准**：它是否帮助用户写作和保护用户判断权，而不是它看起来是否聪明。

## 7. 技术形态（方向倾向，未最终拍板）

> **注意：技术栈目前是"暂选"，不是"已定"。** 用户是开发新手，技术地基必须"亲手碰过、确认不会卡在环境配置这第一步"之后才正式拍板。在那之前，以下都是倾向，不是铁板钉钉。若验证时发现 Electron 对新手太劝退，仍可换更简单的方案。

- 形态：本地桌面应用，能双击打开的独立窗口（不是浏览器网页）。**（这一条较确定）**
- 桌面框架：Electron。**（暂选，待亲手验证环境搭建是否顺利）**
- 语言：TypeScript。**（暂选，随桌面框架一起定）**
- 项目性质：开源 + 跨平台（Windows / macOS / Linux）。**（这一条较确定）**
- 必须包含：接入大语言模型 API。**（这一条确定）**
- 协作模式：代码主要由 AI 助手编写，用户负责产品判断与验收（用户是开发新手）。**（确定）**

## 8. 验证路径（怎么证明第一版成立）

第一版的成功分两层：工程链路能稳定跑通，产品灵魂真的解决"想不出来"。两层都过，第一版才算立住。

### 8.1 工程验收（核心闭环能跑通）

1. **写作链路**：用户打开作品 -> 在草稿本写几句 -> 在正文本写正文 -> 保存 -> 重新打开后内容还在。
2. **选区 AI 链路**：用户选中一段文本 -> 召唤 AI -> AI 能结合选区和上下文给出临时回应。
3. **手动使用链路**：用户复制 AI 面板里的某句话 -> 自己粘贴到草稿本或正文本 -> 保存后可回读。
4. **不越界验证**：AI 全程不自动插入、不自动替换、不自动整理草稿本或正文本。
5. **配置链路**：填入 Key / 模型名 / 接口地址 -> 保存 -> 能成功调用一次模型。

### 8.2 产品验收（灵魂是否成立）

工程能跑通，不等于产品成立。工具的灵魂是"陪用户从卡住到能继续写"，这一层必须单独验：

6. **好问题验收**：拿 3 到 5 个用户真实卡住的片段来测。对每个片段，AI 的输出必须满足：
   - 给出**问题 + 可能性**，帮用户看见这段材料能往哪里长。
   - **不替用户判断**：不默认整段替用户续写正文，不评价材料高级/低级/正确/错误，不把材料塞进剧作模板后命令用户照做。
   - 用户能据此**写下一小段**，而不是只收到一堆泛泛建议。
   - 只要 AI 变成"普通聊天窗"或"默认代写机"，这一条就算不合格。

   注：不要求 AI 写得好，只要求它站在"陪想"而不是"代写/裁判"的位置。

### 8.3 排后增强验收（核心稳定后再验）

7. **内容库验证**：同一段选区，在接入内置 AI 内容库后，AI 的问题/发散明显更贴近剧作创作，而不是普通聊天。
8. **版本链路**：用户保存多个版本 -> 能查看并恢复旧版本 -> 恢复前有影响范围提示。

## 9. 尚未讨论、留待后续

- 具体技术选型细节（Electron 项目结构、状态管理、编辑器组件、本地存储方式等）。
- 草稿本、正文本、AI 面板的具体布局。
- 选区召唤 AI 的交互细节。
- AI 面板默认是问题优先、发散优先，还是混合模式。
- AI 内容库第一版具体包含哪些文档。
- 数据在本地如何持久化（文件还是本地数据库）。
- 版本控制的快照粒度与恢复方式。
- 未来说明性文档的候选整理与用户确认机制。
- 工程基建：git、npm、环境隔离、代码规范、测试用例的具体落地。

这些进入实现前会另行讨论，不在本方向文档拍板。
